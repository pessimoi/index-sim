# Feature inventory

- Status: current snapshot
- Date: 2026-07-06
- Owner: product docs
- Scope: visible legacy-vs-rewrite feature coverage.

## Status meanings

- `Valmis`: Present in the root Vite rewrite UI.
- `Osittainen`: Present, but with reduced controls, reduced parity evidence or missing legacy migration.
- `Suunniteltu`: Known legacy capability or rewrite requirement, not implemented in the root rewrite UI yet.
- `Avoin`: Needs a product decision before implementation.

## Rewrite UI parity

| Feature                      | Rewrite status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Basic combat setup           | `Osittainen`   | Root rewrite UI now exposes the dense spreadsheet compact setup strip: combat type, primary offensive level, melee strength or magic spell, defence, stance, primary prayer, primary boost, read-only override placeholders, attack speed, food pressure and target. Full gear, trip, multi-prayer and multi-boost controls remain planned for the full workbench/parity fill.                                                                                                                                                             |
| Result summary               | `Valmis`       | DPS, max hit, hit chance, effective XP/hr and effective net GP/hr are visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Dense spreadsheet view       | `Osittainen`   | Root rewrite UI has the Phase A dense shell: compact control strip, metric strip, full all-monster sortable table, persisted dense sort, row-click target selection, active row marker, sticky header and contained horizontal overflow. Later parity fill still needs filters, custom setup markers, per-monster state markers and browser-rendered numeric snapshots.                                                                                                                                                                    |
| Loot/economy summary         | `Osittainen`   | Root rewrite UI now exposes the full current-monster drop table, per-drop loot/skip/bury/alch/unid/value controls when applicable, versioned rewrite loot preferences, current-monster reset, deterministic net GP/hr optimization, per-action net GP/hr impact and browser-local price history summary. Top loot and supply metrics remain present. Full economy tab, per-monster overhead/alch placement and talisman spot controls remain open.                                                                                  |
| Trip controls                | `Osittainen`   | Food, bank time, prayer mode, prayer potion auto/manual vials/manual doses, altar timing, teleport, alching, ammo recovery, Ring of Wealth, safespot override, protect prayer, antifire, antipoison, manual food count, food-per-kill override and recoil ring count are present in the root UI. The Trip summary now shows active survival, prayer restore, food and recoil assumptions plus related trip numbers. Scarce/AFK, inventory reserve details and broader potion recommendation controls remain open. |
| Monster compare              | `Osittainen`   | The root rewrite now uses the full dense all-monster table with legacy column order, default XP/hr descending sort, sortable headers and row target selection. Filters, relevance state, custom setup markers and per-monster overhead/alch markers are not ported.                                                                                                                                                                                                                                                                        |
| Planner                      | `Osittainen`   | A rewrite planner summary is present. Legacy planner UI state migration and full legacy planner parity are open.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Special attacks              | `Osittainen`   | Root rewrite UI exposes supported melee/ranged special weapon selection, ranged spec-arrow selection when needed, versioned setup persistence, safe request mapping into `SimulationRequest.specialAttack` and visible special metrics from the existing domain result. Magic special UI, DBA restore expansion, new special formulas and dragon-halberd/NPC-size behavior remain outside this slice.                                                                                                                                          |
| Cannon                       | `Osittainen`   | Root rewrite UI now exposes per-monster cannon enable, target count, respawn, output metrics, cannonballs per trip and cannon-adjusted XP/GP/supply rates in the dense dashboard. Final legacy Cannon-tab placement, full explanatory copy and broader browser-rendered numeric parity remain open.                                                                                                                                                                                                                                        |
| Market price sync            | `Osittainen`   | Rewrite UI has current-monster and all-supported sync controls, same-origin status/sync calls, validated browser adapter, partial failure reporting, explicit `PriceSet` selection, service-aware disabled copy instead of `run_sim.py` instructions and a capped browser-local `index-sim:price-history` writer for accepted imported/synced price sets. Compatible legacy `sim_prices_v1` + `sim_alch_v1` maps can be imported as an explicit `PriceSet`; full legacy price history is only detected. The accepted target source is `markets.lostcity.rs`, with retained 12-hour shared history snapshots once the refresh workflow exists. The repo-owned Vite dev/preview API boundary exists with a disabled default provider; production runtime/hosting remains open. |
| Hiscores                     | `Osittainen`   | Hiscores is required for v1 replacement. Rewrite UI has player input, same-origin status/lookup calls, validated browser adapter, preview and Apply flow for Attack, Strength, Defence, Hitpoints, Prayer, Ranged and Magic, with service-aware disabled copy instead of `run_sim.py` instructions. Compatible legacy `sim_hiscore_player` can be imported into the rewrite-owned last-player key. The repo-owned Vite dev/preview API boundary exists with a disabled default provider; authoritative live API/source and production runtime/hosting remain open. |
| Legacy saved setup migration | `Osittainen`   | Rewrite detects known legacy browser storage keys, shows a user-facing import/keep/clear choice, can import compatible `sim_input_v3` setup fields into `index-sim:rewrite-setup`, compatible `sim_hiscore_player` into `index-sim:hiscores:last-player` and compatible legacy price/alch maps into an explicit browser `PriceSet`. Import keeps legacy data, stores a rewrite-owned dismissed state and Clear removes only known legacy keys after explicit confirmation. Full planner state, custom setup, loot prefs, compare state, cannon map, hidden tiers and full legacy price history migration remain open. |

## Maintenance rule

Update this file when a visible workflow is added, removed, intentionally scoped down or accepted as a legacy-only behavior.
