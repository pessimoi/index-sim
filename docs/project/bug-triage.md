# Bug triage for golden fixtures

- Status: current baseline triage
- Date: 2026-07-10
- Sources: `PROJECT_REVIEW_NOTES.md`, `engine.js`, `trip.js`, `gamedata.js`, `equipment.js`

This document separates known current behavior from rewrite decisions. No legacy behavior was fixed before capturing the current golden fixtures.

## Fixture policy

- Golden fixtures preserve the current `SimEngine.simulate()` behavior temporarily.
- The fixture harness does not load `market.js`, `planner-core.js`, `views.jsx`, browser `localStorage` or live network data.
- Bugs outside `SimEngine.simulate()` are triaged here but not locked as simulation parity.
- No item below is an accepted intentional behavioral change unless a decision records it in [decisions.md](decisions.md).

## Triage list

| Area                 | Known risk                                                                                                                                                                                                                                                       | Baseline handling                                                                                                                                                                                                                                                                        | Rewrite follow-up                                                                                                                                                                                    |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Market sync          | Archived current-monster price sync can crash on nested loot rows.                                                                                                                                                                                               | Preserve in archived evidence; market sync is not loaded in golden fixtures. The rewrite expands nested/tagged ids through validated market mapping and uses scheduled static PriceSets.                                                                                                 | Closed for the supported rewrite path. Reopen the archived behavior only if D-044 legacy-runtime re-promotion is accepted.                                                                           |
| Planner              | Archived Planner can score candidate weapons with stale `accByType`.                                                                                                                                                                                             | Preserve in archived evidence; Planner is outside the legacy simulation fixture set. The root Planner uses `src/domain/planner` and candidate-specific combat data.                                                                                                                      | Closed for the supported rewrite path; deterministic rewrite Planner fixtures cover accepted V1 behavior.                                                                                            |
| Planner              | Archived Planner can select training stance ids based on the base weapon instead of candidate weapon.                                                                                                                                                            | Preserve in archived evidence; Planner is outside the legacy simulation fixture set. The rewrite resolves stance/style from each candidate weapon.                                                                                                                                       | Closed for the supported rewrite path; exact legacy Planner numeric parity remains a separate decision, not a bug-fix prerequisite.                                                                  |
| Operations/API       | Archived legacy UI references missing `run_sim.py` and legacy `/api/*` routes; production rewrite uses same-origin service states with disabled default providers.                                                                                               | Preserve archived legacy evidence outside golden fixtures.                                                                                                                                                                                                                               | D-044 keeps legacy `/api/prices`, `/api/scrape` and legacy `/api/hiscores` shims archive-only for the current rewrite path; production runtime/upstreams remain separate live-integration decisions. |
| Economy/data         | Gem alias prices can update keys that EV code does not read.                                                                                                                                                                                                     | Preserve archived parsed values; no market sync in fixtures. D-058 keeps source-backed cut/uncut gem identities distinct, exact keys win, and the reviewed legacy alias map is missing-key fallback only across rewrite economy paths.                                                   | Closed for the supported rewrite path. New aliases require source evidence and collision-gate coverage.                                                                                              |
| Economy/data         | Archived `SLUG_MAP` covers only part of the item universe and contains stale source slugs.                                                                                                                                                                       | Preserve in archived evidence; no live scrape runs in fixtures. The rewrite uses a bounded allowlist audited against the current market item catalog for scheduled writer and compatibility adapter paths.                                                                               | Closed for current mapped items. Broader market-catalog coverage remains a later scope decision.                                                                                                     |
| Loot/Ring of Wealth  | Code/comment scope differs: Ring of Wealth affects `randomjewel`, not every rare table.                                                                                                                                                                          | Preserve; fixture includes `melee_green_dragon_antifire_ring_of_wealth`.                                                                                                                                                                                                                 | Decide and document intended RoW scope before changing behavior.                                                                                                                                     |
| Trip/incoming damage | Monster incoming damage can be underestimated where monster strength bonuses or max hits are incomplete.                                                                                                                                                         | Preserve; trip fixtures lock current model outputs.                                                                                                                                                                                                                                      | Add explicit monster max-hit/provenance fields during data rewrite.                                                                                                                                  |
| Trip/safespot        | Ranged, magic and halberd default to safespot and therefore zero incoming damage.                                                                                                                                                                                | Preserve under D-046; fixtures include ranged, magic and halberd safespot cases, and the rewrite surfaces safespot assumptions as visible/reviewable Trip state with user override.                                                                                                      | Reopen only if source-backed encounter/NPC data justifies changing the V1 default.                                                                                                                   |
| XP/Recoil            | Legacy reduces combat XP/hr when ring-of-recoil damage contributes to kill speed, while the rewrite keeps combat XP attributed to direct player combat damage.                                                                                                   | Keep `melee_ring_recoil_fire_giant_food_trip` as comparison evidence only.                                                                                                                                                                                                               | Accepted intentional rewrite delta in [D-031](decisions.md); keep the diagnostic XP test rather than returning this fixture to parity.                                                               |
| Specials             | Dragon halberd special applies the second hit to all targets because NPC size data is missing.                                                                                                                                                                   | Preserve under D-050 for V1; fixture includes `melee_dragon_halberd_rock_crab_small_target_spec`; rewrite special metrics show an info warning when this fallback is active.                                                                                                             | Reopen only with source-backed NPC-size data plus an explicit formula/baseline decision.                                                                                                             |
| Ammo/data            | Archived hardcoded `adamant_arrow` fallback can mask price-key problems.                                                                                                                                                                                         | Preserve in archived evidence. The root runtime resolves the source-backed ammo identity and consumes validated scheduled/generated `PriceSet` values.                                                                                                                                   | Closed for the supported rewrite path; live price freshness remains the separate D-053 evidence boundary.                                                                                            |
| Drops/data           | Archived drop names and keys can disagree.                                                                                                                                                                                                                       | Preserve legacy fixture behavior for comparison. The generated Revision 274 runtime resolves reviewed source identities, validates loot shapes and reports zero runtime identity blockers; quest/clue exclusions remain explicit.                                                        | Closed for current core-loot scope. New quest/clue rows require an accepted content policy and generated impact evidence.                                                                            |
| Data literals        | Duplicate price/data object keys can hide earlier values. Current archived `gamedata.js` source duplicate object keys detected on 2026-07-06: `tin_ore`, `copper_ore`, `adamantite_bar`, `spinach_roll`, `bronze_scimitar`, `iron_scimitar` and `adamant_arrow`. | D-045 preserves current parsed JavaScript behavior while `gamedata.js` remains an archive/bootstrap source. Raw JSON duplicate keys, duplicate legacy monster ids, malformed loot and generated/source-backed duplicate canonical item ids from known alias mappings are gated in tests. | Do not add a standalone `gamedata.js` duplicate-object-key hard gate. Resolve canonical duplicate values through generated/source-backed data and the eventual generated runtime snapshot.           |
| Alch data            | Archived zero alch values are not automatically loaded from external files.                                                                                                                                                                                      | Preserve archived behavior in legacy fixtures. The rewrite validates alch values in `PriceSet`, loads scheduled values and completes only missing ids from generated source-backed item data.                                                                                            | Closed for the supported rewrite path.                                                                                                                                                               |

## Open decisions

- Changing Ring of Wealth table scope requires an authoritative game-rule source and an explicit behavior/baseline decision.
- Improving incoming-damage/max-hit modeling requires source-backed monster fields and an explicit formula/baseline decision.
- Quest-gated and clue-scroll tertiary loot remain outside the current core-loot snapshot until their content policy is accepted.
- Archived-only risks are not rewrite work unless a future decision re-promotes the legacy runtime under D-044.
