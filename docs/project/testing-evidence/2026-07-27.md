# Testing evidence — 2026-07-27 DOCS-01

- Status: historical
- Date: 2026-07-27
- Owner: project testing evidence
- Evidence: verified
- Contract: closed

This snapshot records the documentation-structure hardening validation. Counts,
artifact measurements and hashes below apply only to this worktree state.

## Focused validation

- `npm run docs:check` passed with all local file/heading links, documented npm
  commands, metadata owners, navigation depths, lifecycle entries and
  volatile-fact rules valid.
- The documentation-check Vitest file passed all seven focused parser,
  navigation, command, catalog and volatile-fact cases.
- `npm run typecheck`, `npm run architecture:check`, `npm run format:check` and
  `git diff --check` passed.

## Retained timeout and rerun evidence

An initial complete `npm run test` attempt passed every suite except the
maximum-size Duel matrix performance case, which reached its existing timeout
while the full test process was under load. The isolated
`src/tests/ui-performance.test.ts` rerun then passed all three cases without a
test, budget or source change. The subsequent complete repository gate passed
that performance owner in the integrated run.

## Final repository gate

`npm run verify` passed 119 Vitest files with 1,147 tests and the separate 19
legacy goldens, followed by production build, artifact validation, lint,
Prettier and diff validation. The architecture stage reported no cycles,
forbidden dependencies, stale entrypoints or migration exceptions. The build
artifact remained within its accepted entry budgets; its exact measurements and
checksum are build output for this dated run, not living documentation facts.

Network-disabled dependency audit was skipped by the repository's existing
local gate policy. No browser, visual, VoiceOver/NVDA, branded Safari, physical
device, live-provider or deployed Cloudflare evidence was run or claimed.
