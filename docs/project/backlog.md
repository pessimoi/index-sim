# Backlog

Bounded work that can be picked up without inventing a new architecture.

Status date: 2026-07-10.

## Status meanings

- `Done`: implemented and evidenced in the current root Vite rewrite or current docs.
- `Partial`: a useful slice is implemented, but the card still has an explicit remaining gap.
- `Open`: not implemented or not decided yet.
- `Blocked`: waiting for an accepted decision, upstream answer, hosting choice or generator/writer foundation.
- `Conditional`: no active work unless a later release or human decision explicitly requires it.
- `Ongoing`: recurring maintenance; never permanently complete.

## Current-app stabilization

- `Conditional` - Choose production runtime, hosting and authoritative upstream for hiscores before a public/live release claim. This is not a blocker for the current trusted-tester handoff while manual level editing remains the fallback. Market price refresh now has an accepted GitHub Actions scheduled static JSON model, local writer, normalized fixture contract, fixture-evidenced raw adapter and scheduled commit-if-diff workflow, but live response contract verification, `MARKET_PRICES_UPSTREAM_URL` configuration and a verified successful scheduled run remain missing. D-053 keeps that market evidence out of the V1/trusted-tester gate while leaving it required for scheduled-current price claims. The live-integration gap matrix in [../technical/live-integrations-spec.md](../technical/live-integrations-spec.md#live-integration-gap-buckets) separates current handoff safety/copy work from blocked live-claim work, decision-needed runtime choices and D-044 archive-only legacy shim policy.
- `Done` - Accept the current external sharing model as trusted-tester handoff to one known friend. Do not build full CI/CD, final hosting, public release packaging or deploy-target hardening until a later release decision asks for it.
- `Done` - Decide whether archived legacy `run_sim.py`, `/api/prices`, `/api/scrape` and `/api/hiscores` copy needs compatibility shims or can stay archive-only until legacy deletion. D-044 accepts archive-only for the current rewrite path: do not build legacy compatibility shims unless a later explicit legacy-runtime re-promotion decision changes the boundary.
- `Done` - Decide whether to delete the archived index-sim implementation after the source-backed runtime switch. D-060 retains it as regression, reference and historical evidence outside the supported root runtime. No legacy deletion program is active; reconsider removal only after a later explicit decision and app-owned replacements for any still-needed legacy-executing checks.
- `Done` - Add a small static-run section to operations once the preferred local run command is confirmed. [../operations/README.md](../operations/README.md) now documents `npm run dev`, `npm run build`, `npm run preview` and the archived legacy reference path.
- `Done` - Fix the legacy browser planner `accByType`/stance issues described in `PROJECT_REVIEW_NOTES.md`, or replace the UI path with `src/domain/planner`. The root Planner path now uses `src/domain/planner`; the old planner remains archived reference only.
- `Conditional` - Fix archived legacy current-monster price sync for nested loot entries only if the archived legacy runtime is re-promoted to a supported path. The accepted rewrite market path expands nested/tagged items through `src/data/market-sync-items.ts`; the remaining legacy behavior is legacy-only/archive-only under D-044 unless a future legacy-runtime re-promotion decision changes the boundary.
- `Done` - Decide archived `gamedata.js` duplicate object key hard-gate policy. D-045 accepts no standalone merge-blocking hard gate for duplicate object keys in archived legacy JS; preserve current parsed JavaScript behavior while `gamedata.js` remains an archive/bootstrap source, and resolve canonical duplicate values through generated/source-backed data instead. Raw JSON duplicate keys, duplicate legacy monster ids, malformed loot entries and generated/source-backed duplicate canonical item ids from known alias mappings remain covered by tests.
- `Done` - Close canonical gem/item price alias normalization scope for V1 by D-052 and D-058. Revision 274 cut and uncut gem ids remain distinct source item identities. Economy lookup uses the exact item/price key first and falls back through the reviewed legacy gem alias map only when the exact key is absent; alias-map self-collisions remain hard-gated.
- `Done` - Decide whether ranged/magic/halberd auto-safespot remains default UX. D-046 keeps the current ranged, magic and halberd auto-safespot behavior as the V1 default with visible user override, while leaving source-backed encounter/NPC data free to reopen the default later through a separate evidence-backed decision.
- `Done` - Close dragon halberd NPC-size behavior for V1 by D-050. The rewrite keeps the current NPC-size fallback behavior with a visible dragon halberd warning and does not add manual NPC-size data. Source-backed NPC-size data, formula changes or baseline updates require later generated-data evidence plus an explicit decision.

## Rewrite preparation

- `Done` - Implement the accepted v1 replacement scope: combat, result summary, monster compare, loot/economy, trip and planner. Basic combat setup, Result summary, Stats analysis, Dense spreadsheet view, Monster compare, Loot/economy summary, Market price sync, Trip controls, Special attacks and Planner are marked `Valmis` in [../product/feature-inventory.md](../product/feature-inventory.md) for the visible rewrite V1 workflows.
- `Ongoing` - Keep V1 release evidence current after each release-impacting change. The 2026-07-10 D-059 refresh records passing typecheck, full unit (28 files, 468 tests), golden (19 tests), build, dependency audit, deterministic raw Revision 274 generation, source audit, generated and legacy-reference readiness/freshness checks, and all four raw source-impact profiles. Generated runtime readiness has zero blockers; the committed revision-impact report passes 10/10 representative cases and records 22 advisory outliers across 189 evaluations. The full Playwright command was attempted but stopped before browser execution because the managed sandbox cannot bind `127.0.0.1:5173`; the earlier localhost-capable 51/51 run remains historical browser evidence. `npm run lint` now excludes gitignored `.sources` checkouts but exposes 19 existing code errors and 8 warnings, and repo-wide `npm run format:check` reports 62 files; these are explicit tooling-hygiene gaps rather than hidden functional pass claims. No `Open` or `Partial` implementation card remains in this backlog after the source-backed runtime switch.
- `Done` - Resolve the Playwright smoke release blocker for the accepted current rewrite browser scope. The superseded failure matrix in [../technical/testing.md](../technical/testing.md#superseded-playwright-smoke-failure-matrix) is retained as historical triage evidence; it is no longer the active release-gate state.
- `Done` - Collect the current rewrite release blocker rollup from the partial bucket matrices. [../technical/rewrite-parity-report.md](../technical/rewrite-parity-report.md#current-rewrite-release-blocker-list) now separates the current visible release-required boundary from blocked, decision-needed, later and legacy-only work. No general full-legacy-parity, full visual regression, all-fixture browser-display or exact legacy planner numeric parity requirement is added as a release blocker without a future decision.
- `Done` - Finish the accepted Combat/Stats/Special/Result Summary V1 replacement slice. Visible combat setup, Stats analysis, supported special attack workflows, magic/DBA unsupported/suppressed states and the existing Result summary level are implemented in the rewrite architecture with `SimulationRequest` kept separate from UI/persisted setup state. D-050 keeps the dragon halberd NPC-size fallback warning for V1. New special formulas, magic DPS specials, source-backed dragon-halberd/NPC-size data and broader browser-display/visual-regression evidence remain later scope, not blockers for this slice.
- `Done` - Prioritize the missing legacy UI workflows listed in [../product/feature-inventory.md](../product/feature-inventory.md) into parity-required, later enhancement, decision-needed, blocked or legacy-only buckets. The core UI/workbench gap snapshot in [../technical/rewrite-parity-report.md](../technical/rewrite-parity-report.md#core-uiworkbench-gap-buckets) now buckets Basic combat setup, Stats analysis, Loot/economy summary, Trip controls, Monster compare and Special attacks without changing their `Valmis` accepted-slice status. Dense/Compare, Combat/Stats/Special/Result, Loot/Economy and Trip slices are closed for the accepted visible V1 replacement scope, and no current open gap in those six rows is `release-required`.
- `Done` - Ring-of-recoil XP attribution decision is closed by D-031: legacy remains comparison evidence, while rewrite combat XP is not reduced by trip-layer recoil damage. The accepted delta is explicit in XP tests and release evidence.
- `Done` - Finish the accepted Loot/Economy V1 replacement slice. The current-monster action workflow, per-monster loot settings, browser-local Economy tab, Loot value composition, full nested drop detail, readable action-impact detail, browser-local Economy-history context in Loot rows, active/latest price context, Snapshot now, confirmed Clear history, local PriceSet import override and reset-to-scheduled fallback are implemented in the rewrite UI with explicit `PriceSet` state. D-049 keeps full legacy price-history migration out of V1. Live provider evidence, shared/server-managed history and live scheduled-run verification remain later or decision-needed scope, not blockers for this visible V1 slice.
- `Conditional` - Resolve only explicitly accepted exact-legacy Trip parity questions beyond the accepted food/banking, scarce/AFK target/respawn, reserve summary, prayer restore capacity, general potion carry controls, domain-owned potion recommendation/apply flow, grouped Trip summary and Cannon sparse-linking paths. Prayer potion modeling, live data, canonical data, provider decisions and exact archived legacy `potRec` numerical parity remain outside this backlog item unless separately accepted.
- `Conditional` - Resolve only explicitly accepted remaining Special attacks parity questions after the supported melee/ranged DPS-special UI, ranged spec-arrow selection, DBA boost/Trip restore path, magic unsupported state, safe request mapping, persisted-state cleanup and D-050 dragon halberd NPC-size fallback warning. New special formulas, magic DPS specials and source-backed dragon-halberd/NPC-size behavior remain outside this backlog item unless separately accepted with source data.
- `Done` - Cannon final workbench placement is implemented. Further Cannon browser-rendered numeric parity is only conditional on a later release requiring all-fixture browser-display coverage beyond the current Dagannoth dense row, root metric strip and expanded Cannon output snapshots.
- `Done` - Monster Compare / Dense Spreadsheet V1 replacement slice is closed. The visible dense all-monster table, sort/filter, row target selection, active/forced-visible target marker, per-monster markers, persisted rewrite-owned relevance state, visible-row scale indicators, responsive overflow and representative browser numeric evidence are accepted for V1 replacement. D-032 remains the release-path numeric classification; all-fixture browser-display expansion, full visual regression, deeper legacy compare-state migration, final Dense-vs-workbench default UX and worker-backed compare execution remain later or decision-needed scope.
- `Done` - Finish the accepted Planner V1 replacement slice. The workbench Planner tab, rewrite-owned Planner UI state, domain adapter, deterministic rewrite-domain golden plans, gear-pool allowlist/cleanup, generated requirement lookup/fallback warning, visible chart/timeline output and `sim_planner_v1` review-only migration boundary are accepted for the rewrite replacement line. D-047 keeps future/hypothetical gear out of V1, D-048 keeps `sim_planner_v1` review-only/not migrated for V1 and D-051 keeps the manual requirement fallback for V1. Legacy planner numeric parity and authoritative generated requirement source mapping/fallback removal remain separate decision-needed or later work, not blockers for this slice.
- `Conditional` - Add legacy planner UI-state fixtures only if legacy planner numeric parity or legacy planner state migration is explicitly accepted.
- `Ongoing` - Extend mocked live integration fixtures as upstream contracts become authoritative. Initial hiscores and market sync mocked coverage exists for schema/API/adapter/UI paths, but live upstream contracts are not final. Keep automated checks mocked/same-origin until the live-integration bucket matrix promotes a specific provider or writer path out of `blocked`.
- `Done` - Close review-only legacy live-integration `localStorage` migration scope for V1 by D-049. Compatible hiscores last-player and current price/alch import are covered by the legacy import flow, legacy scrape-key metadata is classified as intentional reset on confirmed Clear and full legacy price-history payloads stay review-only/not migrated for V1. Any future full history import, shared/server-managed history or reset-only policy requires a separate explicit product/storage decision.
- `Done` - Replace the legacy runtime snapshot adapter with the authoritative current `GameDataSnapshot`. `src/adapters/generated` now builds the root `SimulationContext` from committed Revision 274 `game-data.json`, scheduled static prices and generated item/alch fallbacks. `npm run runtime:readiness` reports zero blocking ID, required-field, combat, loot or PriceSet coverage gaps. D-059 accepts the bootstrap switch; `src/adapters/static-runtime` remains regression/reference and rollback evidence only.
- `Done` - Implement the accepted game revision bump workflow. `npm run data:generate` recursively reads pinned raw `.npc`, `.obj`, `.dbrow`, `.param` and `.rs2` inputs, resolves every expected runtime identity, extracts 63/63 core-loot tables with 1,101 top-level entries, writes the committed source pin/snapshot/report, enforces path/output hygiene and runs the raw-runtime representative plus informational all-monster impact profiles. The committed report passes 10/10 representative cases with D-055/D-057 accepted deltas. Four quest-gated and 21 clue-scroll tertiary rows remain explicit scoped exclusions; D-051 keeps authoritative requirement-skill inference/fallback removal outside this completed row.
- `Conditional` - Finish the accepted GitHub Actions scheduled `markets.lostcity.rs` price writer live-evidence phase before claiming scheduled-current market prices. The repo now has `npm run prices:write-scheduled`, normalized upstream fixture tests, a fixture-evidenced raw response adapter for the `--upstream-url` path, approved-origin enforcement, deterministic output generation, 12-hour shared history bucket retention, no-op write behavior, `.github/workflows/update-market-prices.yml` for cron at 00:15 and 12:15 UTC with same-repo commit-if-diff wiring through `GITHUB_TOKEN`/`contents: write`, and freshness/release-evidence docs for `_scraped_at`, latest market-file commit, latest successful workflow run, failed-run behavior and no-op runs. D-053 keeps live response contract verification, safe `MARKET_PRICES_UPSTREAM_URL` repo variable setup and first successful scheduled run validation out of the V1/trusted-tester gate; those remain required before scheduled-current price claims.
- `Done` - Replace the browser raw-legacy-data bootstrap with a committed source-backed snapshot. The root app imports `src/adapters/generated`; no normal browser bootstrap executes archived legacy sources or loads the legacy-derived bridge. The static bridge writer/readiness checks remain available for regression/reference evidence. Bundle-size reduction and legacy source deletion stay conditional under the separate deletion decision.
- `Done` - Fold combat/equipment, trip/loot/supply and economy slices into the UI-facing result contract. The combat-only result is explicitly named `CombatSimulationResult`, `src/domain/simulation` defines the composed `FullSimulationResult` foundation with scoped warnings and current XP/rate assembly, the primary `createSimulationViewModel()` path exposes and consumes that composed result, and Dense Compare/Compare plus Duel row generation now read their rate, XP and economy numbers from the same composed result path. Planner remains intentionally combat-focused with explicit `CombatSimulationResult` typing. Whether a later public contract should be renamed back to `SimulationResult` remains an open naming decision, not remaining implementation debt for this backlog row.
- `Conditional` - Extend the rewrite UI to full legacy parity only if a later release explicitly raises the bar beyond the accepted selected parity slice. The current V1 replacement scope is already accepted for the visible workflows tracked in feature inventory.
- `Done` - Implement the accepted Legacy Migration V1 custom setup/cannon import from D-042 and close the user-facing V1 migration status copy. The rewrite detects and classifies every known legacy key in the review UI; shows metadata-only importable/skipped/review-only outcomes; imports compatible `sim_input_v3` active setup fields, nested `sim_input_v3.monsterSetups` into rewrite-owned monster-specific custom setup state, nested `sim_input_v3.cannonByMonster` into rewrite-owned per-monster cannon state, compatible `sim_hiscore_player` last-player state, compatible legacy price/alch maps as an explicit `PriceSet`, compatible `sim_hidden_tiers_v1` flags, compatible `sim_compare_sort_v1`/`sim_irrelevant_v1` dense compare state and compatible `sim_loot_prefs_v1` drop-name preferences that resolve to unambiguous current monster row ids. Existing rewrite custom setup and cannon entries win over conflicting legacy rows. Invalid, unsafe, unknown or ambiguous values are skipped with sanitized reasons. Import keeps legacy data, records dismissed state and Clear removes only known legacy keys after confirmation with the exact clear list shown. D-048 keeps `sim_planner_v1` review-only/not migrated for V1, and D-049 keeps full legacy price-history payloads review-only/not migrated for V1; both are presented as explicit V1 decisions, not ambiguous found-but-not-imported errors.
- `Done` - Finish the generated planner/loadout requirement consumer phase. `GameDataSnapshot` now has a top-level requirement map contract, the source-backed generator parses strict v1 `attack`/`defence`/`ranged`/`magic` item requirements from normalized `items.json`, `weapons.json` and `equipment.json` slices, identical duplicated requirements merge deterministically, conflicting requirement values fail with sanitized `source_slice_invalid`, and Planner candidate filtering, Planner unlock binding, setup/loadout warnings and gear quick action reason copy read `GameDataSnapshot.requirements` when present. D-051 keeps the manual `ITEM_REQUIREMENTS` table as the intentional legacy/missing-generated-data fallback for V1.
- `Done` - Close manual `ITEM_REQUIREMENTS` fallback scope for V1 by D-051. Do not remove or demote the fallback in V1; generated requirements remain primary when present, and the fallback remains only for legacy or missing-generated-data snapshots. Removing or demoting it later requires authoritative upstream field mapping, broader real-source coverage and generated requirement impact evidence, and must not reopen D-047 future/hypothetical gear scope, decide quest requirements, import legacy `sim_planner_v1`, claim legacy planner numeric parity or switch runtime generated snapshots by implication.
- `Done` - Close future/hypothetical planner gear scope for V1 by D-047. Planner V1 remains current-revision-only and uses source-backed or snapshot-backed non-hypothetical gear from the active `GameDataSnapshot` and accepted default planner pool. Future or hypothetical gear requires a separate later product/data provenance decision before any UI exposure, planner pool change or tests are added.
- `Conditional` - Finish market price product acceptance before claiming scheduled-current shared prices in a public release. The rewrite has a disabled-provider same-origin API, validated adapter, scheduled-only UI path, browser-local accepted-price history, read-only scheduled static snapshot loader, local scheduled writer with fixture validation, a fixture-evidenced raw `markets.lostcity.rs` response adapter and scheduled commit-if-diff workflow, but live response contract verification, repo variable configuration and the first successful scheduled run are still missing. The trusted-tester handoff may proceed with documented static/bundled/imported price limitations, and production copy must not claim scheduled-current prices until the latest scheduled run is validated.
- `Done` - Close planner `localStorage` migration scope for V1 by D-048. Legacy `sim_planner_v1` stays detect/review-only and is not parsed or imported into rewrite-owned `index-sim:planner-ui`; Import and Keep preserve the legacy key, and confirmed Clear may remove it with the other known legacy keys. Any future planner-state import requires a separate explicit product/storage decision.
- `Conditional` - Add deploy-target-specific CSP/security headers once hosting is chosen for a real deployment. This is not required for the current trusted-tester handoff.
- `Conditional` - Delete archived legacy runtime files only after full parity, authoritative generated runtime replacement and legacy-deletion decisions are accepted. The root app no longer executes legacy source files during normal bootstrap, but `gamedata.js`, `engine.js` and `equipment.js` still feed golden/reference comparison and the legacy-derived static bridge regeneration path, so deletion remains later replacement-readiness work, not a current handoff blocker.

## Documentation maintenance

- `Ongoing` - Update this backlog when an item moves into active work or becomes obsolete.
- `Ongoing` - Move larger speculative ideas to [idea-inbox.md](idea-inbox.md). Remove stale ideas from the inbox when they become accepted backlog/decision work or are already covered by completed feature inventory.
