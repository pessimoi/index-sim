# Backlog

Bounded work that can be picked up without inventing a new architecture.

Status date: 2026-07-08.

## Status meanings

- `Done`: implemented and evidenced in the current root Vite rewrite or current docs.
- `Partial`: a useful slice is implemented, but the card still has an explicit remaining gap.
- `Open`: not implemented or not decided yet.
- `Blocked`: waiting for an accepted decision, upstream answer, hosting choice or generator/writer foundation.
- `Conditional`: no active work unless a later release or human decision explicitly requires it.
- `Ongoing`: recurring maintenance; never permanently complete.

## Current-app stabilization

- `Conditional` - Choose production runtime, hosting and authoritative upstream for hiscores before a public/live release claim. This is not a blocker for the current trusted-tester handoff while manual level editing remains the fallback. Market price refresh now has an accepted GitHub Actions scheduled static JSON model and a local writer/fixture-contract foundation, but the workflow wiring and exact raw upstream adapter remain missing. The live-integration gap matrix in [../technical/live-integrations-spec.md](../technical/live-integrations-spec.md#live-integration-gap-buckets) separates current handoff safety/copy work from blocked provider/writer work and decision-needed runtime/shim choices.
- `Done` - Accept the current external sharing model as trusted-tester handoff to one known friend. Do not build full CI/CD, final hosting, public release packaging or deploy-target hardening until a later release decision asks for it.
- `Open` - Decide whether archived legacy `run_sim.py`, `/api/prices`, `/api/scrape` and `/api/hiscores` copy needs compatibility shims or can stay archive-only until legacy deletion.
- `Done` - Add a small static-run section to operations once the preferred local run command is confirmed. [../operations/README.md](../operations/README.md) now documents `npm run dev`, `npm run build`, `npm run preview` and the archived legacy reference path.
- `Done` - Fix the legacy browser planner `accByType`/stance issues described in `PROJECT_REVIEW_NOTES.md`, or replace the UI path with `src/domain/planner`. The root Planner path now uses `src/domain/planner`; the old planner remains archived reference only.
- `Conditional` - Fix archived legacy current-monster price sync for nested loot entries only if the archived legacy runtime is re-promoted to a supported path. The accepted rewrite market path expands nested/tagged items through `src/data/market-sync-items.ts`; the remaining legacy behavior is legacy-only/archive-only until the broader legacy deletion or compatibility-shim decision is made.
- `Blocked` - Extend the data reliability gate after a canonical data decision. Raw JSON duplicate keys, duplicate legacy monster ids and malformed loot entries are tested, but archived `gamedata.js` duplicate object keys still need a canonical value decision before that file can be hard-gated. The generated-data-dependent bucket matrix in [../technical/rewrite-parity-report.md](../technical/rewrite-parity-report.md#legacy-migration-and-data-dependent-gap-buckets) keeps this blocked until source-backed canonical values exist.
- `Blocked` - Finish canonical gem/item price alias normalization after the data-source workflow is accepted. Known gem/item alias and fallback price paths surface structured warnings near Result, Loot and Economy money values, but canonical key normalization remains open. The generated-data-dependent bucket matrix classifies this as blocked on authoritative data/source workflow evidence.
- `Open` - Decide whether ranged/magic/halberd auto-safespot remains default UX.
- `Partial` - Add NPC size data or document an intentional dragon-halberd special delta. The rewrite surfaces a dragon halberd NPC-size fallback info warning, but final NPC-size data/formula behavior is still undecided. The generated-data-dependent bucket matrix keeps source-backed NPC-size data blocked before any behavior or baseline decision.

## Rewrite preparation

- `Done` - Implement the accepted v1 replacement scope: combat, result summary, monster compare, loot/economy, trip and planner. Basic combat setup, Result summary, Stats analysis, Dense spreadsheet view, Monster compare, Loot/economy summary, Market price sync, Trip controls, Special attacks and Planner are marked `Valmis` in [../product/feature-inventory.md](../product/feature-inventory.md) for the visible rewrite V1 workflows.
- `Ongoing` - Keep V1 release evidence current after each release-impacting change. The 2026-07-08 Goal 7 refresh records passing typecheck, full unit, golden, build, dependency audit, static security searches and release-copy classification. The current managed-sandbox `npm run test:e2e` rerun still stops before browser execution with `listen EPERM 127.0.0.1:5173`; this is environment-only and not a Playwright assertion failure, but it is not a fresh browser pass for the current checkout. The latest localhost-capable default browser gate remains the earlier 51 out of 51 pass in about 6.0 minutes. D-042 compatible nested legacy custom setup/cannon import is now implemented and unit-evidenced; refresh the legacy migration browser smoke in a localhost-capable environment before a public release package or when the trusted-tester handoff notes need fresh browser evidence. The numeric snapshot audit found no active browser-rendered numeric mismatch, changed no expectations or golden fixtures and keeps future numeric value changes gated on domain/golden/parity evidence or an accepted baseline decision. Planner V1 acceptance has rewrite-domain golden evidence for melee unlock, ranged unlock, magic spell unlock and boosted sustained training; legacy planner numeric parity remains a separate decision if a legacy-clone line is requested.
- `Done` - Resolve the Playwright smoke release blocker for the accepted current rewrite browser scope. The superseded failure matrix in [../technical/testing.md](../technical/testing.md#superseded-playwright-smoke-failure-matrix) is retained as historical triage evidence; it is no longer the active release-gate state.
- `Done` - Collect the current rewrite release blocker rollup from the partial bucket matrices. [../technical/rewrite-parity-report.md](../technical/rewrite-parity-report.md#current-rewrite-release-blocker-list) now separates the current visible release-required boundary from blocked, decision-needed, later and legacy-only work. No general full-legacy-parity, full visual regression, all-fixture browser-display or exact legacy planner numeric parity requirement is added as a release blocker without a future decision.
- `Done` - Finish the accepted Combat/Stats/Special/Result Summary V1 replacement slice. Visible combat setup, Stats analysis, supported special attack workflows, magic/DBA unsupported/suppressed states and the existing Result summary level are implemented in the rewrite architecture with `SimulationRequest` kept separate from UI/persisted setup state. New special formulas, magic DPS specials, final dragon-halberd/NPC-size behavior and broader browser-display/visual-regression evidence remain later or decision-needed scope, not blockers for this slice.
- `Done` - Prioritize the missing legacy UI workflows listed in [../product/feature-inventory.md](../product/feature-inventory.md) into parity-required, later enhancement, decision-needed, blocked or legacy-only buckets. The core UI/workbench gap snapshot in [../technical/rewrite-parity-report.md](../technical/rewrite-parity-report.md#core-uiworkbench-gap-buckets) now buckets Basic combat setup, Stats analysis, Loot/economy summary, Trip controls, Monster compare and Special attacks without changing their `Valmis` accepted-slice status. Dense/Compare, Combat/Stats/Special/Result, Loot/Economy and Trip slices are closed for the accepted visible V1 replacement scope, and no current open gap in those six rows is `release-required`.
- `Done` - Ring-of-recoil XP attribution decision is closed by D-031: legacy remains comparison evidence, while rewrite combat XP is not reduced by trip-layer recoil damage. The accepted delta is explicit in XP tests and release evidence.
- `Done` - Finish the accepted Loot/Economy V1 replacement slice. The current-monster action workflow, per-monster loot settings, browser-local Economy tab, Loot value composition, full nested drop detail, readable action-impact detail, browser-local Economy-history context in Loot rows, active/latest price context, Snapshot now, confirmed Clear history, local PriceSet import override and reset-to-scheduled fallback are implemented in the rewrite UI with explicit `PriceSet` state. Live provider evidence, shared/server-managed history, full legacy price-history migration, raw upstream market adapter and GitHub Actions cron wiring remain later or decision-needed scope, not blockers for this visible V1 slice.
- `Conditional` - Resolve only explicitly accepted exact-legacy Trip parity questions beyond the accepted food/banking, scarce/AFK target/respawn, reserve summary, prayer restore capacity, general potion carry controls, domain-owned potion recommendation/apply flow, grouped Trip summary and Cannon sparse-linking paths. Prayer potion modeling, live data, canonical data, provider decisions and exact archived legacy `potRec` numerical parity remain outside this backlog item unless separately accepted.
- `Conditional` - Resolve only explicitly accepted remaining Special attacks parity questions after the supported melee/ranged DPS-special UI, ranged spec-arrow selection, DBA boost/Trip restore path, magic unsupported state, safe request mapping, persisted-state cleanup and dragon halberd NPC-size fallback warning. New special formulas, magic DPS specials and final dragon-halberd/NPC-size behavior remain outside this backlog item unless separately accepted with source data.
- `Done` - Cannon final workbench placement is implemented. Further Cannon browser-rendered numeric parity is only conditional on a later release requiring all-fixture browser-display coverage beyond the current Dagannoth dense row, root metric strip and expanded Cannon output snapshots.
- `Done` - Monster Compare / Dense Spreadsheet V1 replacement slice is closed. The visible dense all-monster table, sort/filter, row target selection, active/forced-visible target marker, per-monster markers, persisted rewrite-owned relevance state, visible-row scale indicators, responsive overflow and representative browser numeric evidence are accepted for V1 replacement. D-032 remains the release-path numeric classification; all-fixture browser-display expansion, full visual regression, deeper legacy compare-state migration, final Dense-vs-workbench default UX and worker-backed compare execution remain later or decision-needed scope.
- `Done` - Finish the accepted Planner V1 replacement slice. The workbench Planner tab, rewrite-owned Planner UI state, domain adapter, deterministic rewrite-domain golden plans, gear-pool allowlist/cleanup, manual requirement-policy warning, visible chart/timeline output and `sim_planner_v1` review-only migration boundary are accepted for the rewrite replacement line. Legacy planner numeric parity, full `sim_planner_v1` migration/import, authoritative generated requirement extraction and future/hypothetical gear policy remain separate decision-needed or blocked work, not blockers for this slice.
- `Conditional` - Add legacy planner UI-state fixtures only if legacy planner numeric parity or legacy planner state migration is explicitly accepted.
- `Ongoing` - Extend mocked live integration fixtures as upstream contracts become authoritative. Initial hiscores and market sync mocked coverage exists for schema/API/adapter/UI paths, but live upstream contracts are not final. Keep automated checks mocked/same-origin until the live-integration bucket matrix promotes a specific provider or writer path out of `blocked`.
- `Open` - Decide whether review-only legacy live-integration `localStorage` areas should get deeper migration, especially full price history. Compatible hiscores last-player and current price/alch import are covered by the legacy import flow; legacy scrape-key metadata is classified as intentional reset on confirmed clear.
- `Blocked` - Replace the legacy runtime snapshot adapter with a staged authoritative normalized current `GameDataSnapshot` from the current accepted LostCityRS/Content revision once the `npm run data:generate` workflow is implemented. The legacy/data-dependent bucket matrix keeps this blocked until parser, calculation-impact and runtime-switch evidence exists.
- `Partial` - Implement the accepted game revision bump workflow: `npm run data:generate` now has a repository-local source/output-path validation foundation, fixture coverage for the gitignored `.sources/lostcity-content/` source path, a repo-local generated-output hygiene assertion and first committed schema-valid `src/data/generated/source-pin.json`, `src/data/generated/game-data.json` and `docs/project/revision-impact/current.md` foundation outputs. Still open: authoritative parser, branch/PR revision bump process, simulator-consumed normalized domain data extraction from accepted upstream files, generated snapshot diff review, validation, merge-blocking hybrid representative suite, informational all-monster scan with fixed baseline/threshold policy and explicit accepted calculation deltas.
- `Partial` - Implement the accepted GitHub Actions scheduled `markets.lostcity.rs` price writer: the repo now has `npm run prices:write-scheduled`, normalized upstream fixture tests, deterministic output generation, 12-hour shared history bucket retention and no-op write behavior. Still open: GitHub Actions cron at 00:15 and 12:15 UTC, same-repo commit wiring with `GITHUB_TOKEN`, and the exact raw live upstream response adapter.
- `Blocked` - Replace the browser raw-legacy-data bootstrap with a generated static snapshot to reduce Vite bundle size.
- `Partial` - Fold combat/equipment, trip/loot/supply and economy slices into the final UI-facing `SimulationResult` contract. The current view model composes these slices for the UI, but the final public/full-domain `SimulationResult` shape remains open.
- `Open` - Extend the rewrite UI to full legacy parity after the selected parity slice is accepted.
- `Done` - Implement the accepted Legacy Migration V1 custom setup/cannon import from D-042. The rewrite detects and classifies every known legacy key in the review UI; imports compatible `sim_input_v3` active setup fields, nested `sim_input_v3.monsterSetups` into rewrite-owned monster-specific custom setup state, nested `sim_input_v3.cannonByMonster` into rewrite-owned per-monster cannon state, compatible `sim_hiscore_player` last-player state, compatible legacy price/alch maps as an explicit `PriceSet`, compatible `sim_hidden_tiers_v1` flags, compatible `sim_compare_sort_v1`/`sim_irrelevant_v1` dense compare state and compatible `sim_loot_prefs_v1` drop-name preferences that resolve to unambiguous current monster row ids. Existing rewrite custom setup and cannon entries win over conflicting legacy rows. Invalid, unsafe, unknown or ambiguous values are skipped with sanitized reasons. Import keeps legacy data, records dismissed state and Clear removes only known legacy keys after confirmation with the exact clear list shown. `sim_planner_v1` and full legacy price-history payloads are intentionally review-only/not migrated for V1; future migration for either needs a separate decision.
- `Blocked` - Replace manual planner item requirements with generated requirement data once the source workflow is accepted. The generated-data-dependent bucket matrix keeps this blocked until requirement schema, source parsing and impact evidence exist.
- `Open` - Decide whether future/hypothetical planner gear belongs in the product, and if so model it with explicit provenance.
- `Conditional` - Finish market price product acceptance before claiming scheduled-current shared prices in a public release. The rewrite has a disabled-provider same-origin API, validated adapter, scheduled-only UI path, browser-local accepted-price history, read-only scheduled static snapshot loader and local scheduled writer with fixture validation, but the raw `markets.lostcity.rs` response adapter and GitHub Actions cron/commit path are still missing. The trusted-tester handoff may proceed with documented static/bundled/imported price limitations, and production copy must not claim scheduled-current prices until the workflow exists and its latest run is validated.
- `Open` - Decide planner `localStorage` migration scope and whether old planner state should be imported, reset or intentionally left behind. Until that decision exists, `sim_planner_v1` stays detect/review-only and is not imported into rewrite Planner state.
- `Conditional` - Add deploy-target-specific CSP/security headers once hosting is chosen for a real deployment. This is not required for the current trusted-tester handoff.
- `Blocked` - Delete archived legacy runtime files only after full parity, data-generation and replacement decisions are accepted.

## Documentation maintenance

- `Ongoing` - Update this backlog when an item moves into active work or becomes obsolete.
- `Ongoing` - Move larger speculative ideas to [idea-inbox.md](idea-inbox.md).
