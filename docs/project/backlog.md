# Backlog

Bounded work that can be picked up without inventing a new architecture.

Status date: 2026-07-19.

## Status meanings

- `Done`: implemented and evidenced in the current root Vite rewrite or current docs.
- `Specced`: implementation boundaries and acceptance evidence are defined, but source work is pending.
- `Partial`: a useful slice is implemented, but the card still has an explicit remaining gap.
- `Open`: not implemented or not decided yet.
- `Blocked`: waiting for an accepted decision, upstream answer, hosting choice or generator/writer foundation.
- `Conditional`: no active work unless a later release or human decision explicitly requires it.
- `Ongoing`: recurring maintenance; never permanently complete.

## Release-readiness finishing

These six cards are ordered by user impact. They harden existing `Valmis`
workflows and do not reopen feature-parity scope.

- `Done` - Implement [the local-state attention surface specification](../technical/local-state-attention-surface-spec.md). Every ready pane now shares one structured non-dismissible metadata-only notice for unusable stored data, non-durable persistence or both; its only action activates Settings and focuses Local state recovery. Focused view-model/controller/pane suites pass 38/38 and targeted Chromium coverage passes 3/3 without changing recovery blocks, storage schemas or D-016/D-030 migration policy.
- `Done` - Implement [the setup replacement Review/Undo specification](../technical/setup-replacement-review-undo-spec.md). A strict latest-request-wins controller now prepares one in-memory metadata-only candidate without mutation authority; Dismiss changes nothing, explicit Apply replaces all six setup families and both imported Apply and saved-row Load expose one complete persistence-aware Undo. Focused suites pass 28/28 and targeted Chromium transactions pass 3/3 without changing setup, persistence, Duel collection or share schemas.
- `Done` - Implement the shared [numeric input draft and validation contract](../technical/numeric-input-draft-validation-spec.md). Required integer, decimal and optional fields now retain literal drafts, expose accessible unapplied errors and emit only finite in-range step-aligned values; Enter/blur, Escape, Reset and external updates share one lifecycle while all existing domain ranges and formulas stay unchanged. Focused core/component coverage passes 20/20, the production-preview numeric workflow passes, numeric audit reports 5,958/5,958 matching paths and all 19 goldens pass.
- `Done` - Implement [the Planner XP and target integrity specification](../technical/planner-xp-target-integrity-spec.md). Version-1 zero now renders as explicit Auto-at-level-floor, incompatible XP resets visibly to Auto, unlocked targets cannot remain below the live level, locked saved targets are clearly inactive and level 99 accepts XP through 200,000,000. Draft and last-computed snapshots reconcile before the existing one-shot Worker source without changing the explicit Recompute boundary. Focused Planner suites pass 43/43, parity and 19/19 goldens pass, and production-preview browser paths cover manual, saved setup, Hiscores, import and shared-setup changes plus Undo.
- `Done` - Implement [the Duel matrix failure-state specification](../technical/duel-matrix-failure-state-spec.md). One controller-owned presentation model now distinguishes never-built, building, current, stale and failed matrices across all six strict source references; visible fixed-copy Retry never leaks raw Worker details, and stale, rebuilding or failed refreshes retain the latest successful table only as explicitly previous output. Focused controller/pane/Worker coverage passes 17/17 and targeted production-preview Chromium coverage passes 2/2 without changing formulas, Worker protocol or persistence.
- `Done` - Surface the accepted game revision and carry it through setup transfers according to [the game revision and setup-transfer context specification](../technical/game-revision-transfer-context-spec.md). The generated snapshot/source pin agree on explicit Revision 274 context, root readiness requires it, and the ready header plus Settings expose the sanitized active context. One strict stamp/comparison owner now drives dedicated contextual rewrite-setup and saved-setup envelopes plus share v2; setup Apply, saved-setup Merge and share Load remain review-gated, while setup-v3, saved-setup-v1 and share-v1 inputs receive truthful legacy context. Focused suites pass 102/102 and five combined plus one follow-up Chromium transactions pass without changing browser persistence versions, recipient PriceSet/runtime ownership or compatibility rejection.

## Current-app stabilization

- `Done` - Implement [the Dense/Planner/Risk calculation failure and Retry lifecycle](../technical/calculation-failure-retry-lifecycle-spec.md). All three controllers now use explicit `idle/building/ready/stale/failed` presentation, retain and label the latest successful output during refresh and failure, expose fixed visible Retry without raw Worker details, reject late obsolete settlement by task identity plus the exact current source/active eligibility and keep only `ready` Risk output eligible for cross-pane bridges. Focused controller/pane/Worker coverage passes 39/39, full unit coverage 865/865 plus 19/19 goldens, the combined first/refresh-failure Chromium workflow 1/1 and the complete production-preview Chromium suite 88/88 without changing formulas, Worker protocol or persistence.
- `Done` - Implement [the application error boundary and session-only recovery specification](../technical/application-error-boundary-spec.md). The root React boundary now replaces post-ready render/lifecycle failures with fixed sanitized recovery UI, while the bootstrap branch uses the same stable surface with its own message. Normal reload never mutates storage; the safe action reloads the current tab against isolated in-memory defaults, shows a session-only notice and leaves original saved data untouched. Focused unit/jsdom coverage separates bootstrap and ready-pane failures, and a production-preview Chromium path proves the complete failure-to-safe-session transaction without changing domain or persisted schemas.
- `Done` - Implement the high-priority [safe active setup reset](../technical/active-setup-reset-spec.md). The action builds a canonical Revision 274 candidate without mutation, preserves the current target, selected style and setup mode, resets all three per-style loadout caches plus the active Default/current-target Custom owner, presents a grouped Review/Confirm step and uses the existing six-family persistence-aware Apply/Undo transaction. Saved Duel setups, unrelated custom setups, Dense, Cannon, loot, PriceSet, manual prices, histories and Hiscores remain protected; focused and browser evidence covers durable reload, complete Undo and session-only truthfulness.
- `Done` - Complete the incremental `src/app/App.tsx` split according to [the phased composition-root specification](../technical/app-composition-root-refactor-spec.md). Phase 1, presenter hygiene, ten controller slices, Cannon, MonsterCard, all Phase 3 pane families and [the final Phase 4 shell/ownership review](../technical/app-composition-root-phase4-spec.md) are implemented without calculation, persistence-schema, copy, CSS or visual-baseline changes. App is 2,534 lines with the same 46 state cells and retains browser bootstrap, seven persistence effects, multi-feature transactions and direct pane composition. Architecture is 114/102 without cycles/exceptions; focused regressions pass 278/278, numeric paths 5,958/5,958, goldens 19/19, focused/full Chromium 19/19 and 77/77, and read-only visual comparison 20/20.
- `Done` - Implement [the App composition-root Phase 4 final shell and ownership review](../technical/app-composition-root-phase4-spec.md). DOM-free app-shell/legacy owners and four pure shell components now own the moved presentation; Loot/Duel/Loadout helpers have direct owners and obsolete App forwarding code is removed. App retains the intentional state/effect/transaction boundary, and the characterized Duel-only legacy readiness discrepancy remains a separate behavior question. The D-094 entry remains within budget at 720,793 raw / 208,801 gzip bytes.
- `Done` - Complete the 2026-07-13 multi-pass architecture audit recorded in [the repository audit snapshot](../../ARCHITECTURE_AUDIT.md). The audit covers topology, ownership/state/domain boundaries, data/runtime/performance/testing/deploy/security and a final documentation cross-check. It adds an explicit orphan-module/external-entrypoint graph guard, removes the unused broad `src/data` root barrel, extracts the pure MonsterCard rail and refreshes living architecture/testing truth without changing calculations, persistence, APIs or UI copy. Full verify passes 695 tests plus 19 goldens, functional Chromium 77/77, Darwin visual 20/20 against 31 baselines and dependency audit with zero vulnerabilities.
- `Done` - Complete [the 2026-07-14 current production-path architecture audit](architecture-audit.md). The 114-source/102-client module graph has no cycles, layer violations, exceptions, orphan modules or stale entrypoint classifications; domain, generated-runtime, Worker and archive-only boundaries match code. No P0/P1 defect was found. The audit promotes the 1,799-line legacy migration concentration into the living risk list, keeps evidence-free move-only splits conditional and corrects navigation that previously presented the historical 2026-07-13 snapshot as the latest audit. Focused migration coverage passes 44/44 and full verify passes 65 test files / 764 tests plus 19 goldens, typecheck, build/artifact, lint, format and diff gates.
- `Done` - Complete [the 2026-07-14 production-path security audit](security-audit.md). No critical/high issue was found. The scheduled market workflow now pins official actions to immutable commit SHAs, keeps checkout credentials non-persistent and exposes the write token only to the final push step; focused regression coverage owns that contract.
- `Done` - Complete [the 2026-07-14 production-path code audit](code-audit.md). No critical, high or medium defect was found. The audit fixes live Economy history-age refresh, minute-rollover duration formatting, empty persisted-value recovery classification and a broad persistence-effect lint suppression. Full verify passes 67 test files / 767 tests plus 19 goldens, and the complete production-preview Chromium suite passes 78/78.
- `Done` - Implement D-097's disabled-by-default [strict global Hiscores provider-budget path](../technical/hiscores-global-rate-limit-spec.md). The runtime-neutral handler now checks an asynchronous fail-closed gate after validation/local limiting; the Cloudflare Worker injects one deterministic SQLite Durable Object that persists aggregate fixed-window state without player/query/client identity. `wrangler.jsonc` owns the binding and v1 migration with mode `off`, and the pinned Node 22 Wrangler dry-run validates the deploy bundle.
- `Done` - Rebaseline D-094's direct entry-JavaScript release budgets under D-098. The deterministic artifact gate now allows 800,000 raw / 230,000 gzip bytes, restoring maintenance headroom over the 721,534 / 209,167 entry while retaining the asynchronous generated-runtime boundary and rejecting the 1,562,480-byte pre-split shape. Full verify passes 80 files / 787 tests plus 19 goldens; the unchanged deferred chunk is 880,362 bytes, and five paired startup runs record 236/167 ms cold/warm app-ready medians as workstation evidence rather than a universal SLA.
- `Done` - Disable the repository's only GitHub Actions workflow under D-099 because the current maintainer has no remaining Actions capacity. The hardened scheduled market template now lives outside `.github/workflows`, the workflow-security suite guards that no active YAML workflow exists, and Economy tells users that automatic upstream refresh is disabled while committed/imported/manual price paths remain available. Re-enabling is conditional on an explicit capacity and freshness-policy decision.
- `Conditional` - Activate D-097 enforcement or add the specification's location-scoped WAF abuse rule only if a provider quota, public deployment or adversarial-traffic evidence requires it. Activation still needs an accepted positive quota compatible with the fixed 60-second window, D-065 account/privacy evidence, singleton load/latency review and deployed rollback proof.
- `Conditional` - Split scheduled market validation and privileged push into different jobs/runners only if the threat model requires isolation stronger than immutable action pins, lockfile install, non-persistent checkout credentials, final-step token scope and the three-file diff allowlist. Preserve deterministic no-op behavior and do not introduce broad artifact retention.
- `Done` - Implement [the Stats/Loadout pane and view-model refactor](../technical/stats-loadout-pane-refactor-spec.md). Pure pane owners now render Stats and the active Loadout/special/distribution family; direct feature modules own Stats, Loadout and active-assumption builders without mutual imports or compatibility barrels. `App` retains live form mutation and optimizer Apply/Undo. Focused coverage passes 150/150, numeric paths 5,958/5,958, focused Chromium 4/4, full Chromium 77/77, visual comparison 20/20 against the same 31 Darwin PNGs and full verify 697 tests plus 19 goldens. The visual fallback used the standard E2E web-server path because the managed sandbox denied the canonical dedicated port; no baseline or config changed.
- `Done` - Implement [the Compare/Duel pane, view-model and calculation-controller refactor](../technical/compare-duel-pane-refactor-spec.md). Direct feature modules now own Compare/Dense and Duel builders, focused hooks own the unchanged Dense/Duel-matrix lifecycles and pure panes own both feature landmarks while `App` keeps form/snapshot mutation, persistence, recovery and Undo. Focused suites pass 158/158, numeric paths 5,958/5,958, focused Chromium 9/9, complete Chromium 77/77 and full `npm run verify` 704 tests plus 19 goldens. Architecture is 94 modules / 82 client-reachable with no cycle or exception; the entry remains inside D-094 at 705,080 raw / 204,250 gzip bytes. A fresh visual run was blocked before browser execution by managed-sandbox localhost permissions; no baseline or permanent config changed.
- `Done` - Implement [the Planner pane, view-model and calculation-controller refactor](../technical/planner-pane-refactor-spec.md). Direct feature owners now contain Planner adapters/presentation contracts, the explicit-Recompute Worker lifecycle and the complete pure pane while `App` keeps versioned draft state, persistence/recovery and bounded mutations. Focused tests pass 129/129, parity 16 cases / 32 comparisons with zero open rows, complete Chromium 77/77, read-only Darwin visual 20/20 and full verify 710 tests plus 19 goldens. Architecture is 97 modules / 85 client-reachable with no cycle or exception; the entry remains inside D-094 at 706,650 raw / 205,198 gzip bytes.
- `Done` - Implement [the Risk pane, view-model and analysis-controller refactor](../technical/risk-pane-refactor-spec.md). Direct owners now contain the Risk UI contracts/formatters, transient controls, latest build, active task, exact freshness, explicit Run/Cancel lifecycle and complete landmark. `App` keeps live form/context/cannon/loot state, global status and four fresh-only TTK/net-GP/Trip bridges. Focused tests pass 161/161, numeric paths 5,958/5,958, goldens 19/19, complete Chromium 77/77 and read-only visual 20/20; architecture is 100 modules / 88 client-reachable with no cycle or exception, and full verify passes 719 tests.
- `Done` - Implement [the Loot and Trip pane and view-model refactor](../technical/loot-trip-pane-refactor-spec.md). Direct cycle-free owners now contain the shared simulation-input mapping, Loot presentation/optimizer, Trip controls/output and both complete pure landmarks. `App` keeps the versioned setup form and per-monster loot state, persistence/recovery/share replacement, normalized mutation, global Undo/status, Active assumptions and cross-pane bridges. Focused tests pass 238/238, numeric paths 5,958/5,958, focused/full Chromium 13/13 and 77/77, read-only visual 20/20, architecture 105/93 without cycles/exceptions and full verify 730 tests plus 19 goldens.
- `Done` - Implement [the Economy and Settings pane and price-data view-model refactor](../technical/economy-settings-pane-refactor-spec.md). The exact shared wrapper/DOM, active/scheduled/manual/history presentation, neutral Loot history context and hidden-tier presentation now have pure direct owners while App keeps PriceSet/recovery controllers, browser persistence transactions, cross-pane state and topbar bridges. The new suites pass 16/16, the combined focused gate 132/132, numeric paths 5,958/5,958, goldens 19/19, focused/full Chromium 8/8 and 77/77, read-only Darwin visual comparison 20/20 and full verify 746 tests. Architecture is 108/96 without cycles/exceptions, and the entry remains inside D-094 at 716,243 raw / 208,034 gzip bytes.
- `Done` - Implement [the Simulation and MonsterCard view-model refactor specification](../technical/simulation-monster-card-view-model-refactor-spec.md). The 213-line `simulation.ts` now exports only its main composition contract and reuses `FullSimulationResult.combat`; the direct 278-line `monster-card.ts` owner contains the standalone/supplied-combat builders, private detail types and sorted target options without compatibility re-exports. Six focused cases move out of the large view-model suite while one composed assertion remains. Architecture is 115/103 without cycles, exceptions or orphans; focused coverage passes 117/117, numeric paths 5,958/5,958, goldens 19/19, full Chromium 78/78, read-only visual comparison 20/20 and full verify 68 files / 770 tests.
- `Done` - Implement [the legacy migration internal split](../technical/legacy-migration-internal-split-spec.md) after explicit activation of ARCH-2026-02. The unchanged public owner is now a 92-line ordered facade over six coherent internal modules; exact detection/clear policy, metadata-only inspection, rewrite-owned precedence, D-048/D-049 review-only handling and caller-owned atomic Apply remain intact. The former 1,256-line unit owner is split into policy 7, setup 14, preferences 13 and prices 7 cases; with the three view-model cases, focused coverage remains 44/44. Architecture is 121/109 without cycles or exceptions, full verify passes 71 files / 770 tests plus 19 goldens and focused/full Chromium pass 3/3 and 78/78.
- `Done` - Implement [the App composition-root retention specification](../technical/app-composition-root-retention-spec.md) for ARCH-2026-03. Source inspection confirms the current 2,573-line owner still has 46 state cells, seven persistence effects plus bootstrap/timer effects, typed controller outcomes, global status/Undo and caller-owned setup/share/legacy transactions. No defect, cycle, exception or isolation failure justified another extraction, so source stays unchanged; the spec records concrete reopen triggers and rejects brittle line/hook-count guards and evidence-free broad reducers, contexts or catch-all hooks.
- `Conditional` - Split `src/domain/trip/index.ts` only after [the implemented Trip retention and split-trigger specification](../technical/trip-domain-retention-spec.md) is reopened by a concrete defect/conflict, repeated cross-seam change burden, dependency pressure or test-isolation failure. The module has recognizable seams and a stable barrel; size and internal-only exports do not alone justify a move-only refactor. Preserve formulas, PriceSet/persistence policy, public result shapes and golden/numeric evidence if activated. Dated size and test measurements remain in the linked assessment and [testing evidence](testing-evidence.md).
- `Conditional` - Split `scripts/game-data-generator-core.ts` only after [the implemented generator retention and split-trigger specification](../technical/game-data-generator-core-retention-spec.md) is reopened by concrete reuse, change-scatter, dependency or test-isolation evidence. Raw LostCity parsing is already split and production code does not import the script core. Preserve path/output hygiene, source pinning and generated artifact bytes if activated. Dated size and test measurements remain in the linked assessment and [testing evidence](testing-evidence.md).
- `Done` - Measure worker startup plus structured-clone cost according to [the calculation-worker measurement specification](../technical/calculation-worker-measurement-spec.md) and implement [the D-095 retention/persistent-trigger contract](../technical/calculation-worker-retention-spec.md). Five paired production-worker runs cover typical/heavy Dense Compare, Planner, Duel matrix and Risk profiles with aligned clocks. Warm full-request posting is only 1.6-2.3 ms median, startup/delivery 31.8-56.7 ms and non-execution overhead about 34-59 ms. The four-kind raw protocol, 30-second hard-cancel lifecycle and feature-specific freshness policies remain unchanged; the direct gate passes 22/22 and full verify 71 files / 770 tests plus 19 goldens. Persistent context is deferred until a quantified accepted-budget, repeated-demand, clone or resource trigger is met, and any future candidate must use explicit context/request identity plus Worker termination for active cancellation.
- `Done` - Implement [the ARCH-2026-05 feature test-suite split](../technical/feature-test-suite-split-spec.md) for the former 5,046-line functional Playwright owner, 3,562-line combined view-model suite and 1,892-line mixed current/historical testing document. Functional coverage now lives in eight focused specs plus the existing shareable-setup spec behind one fixture module; all 78 Chromium scenarios pass with unchanged names. The 90 combined view-model cases now live in eight focused suites plus the formatting owner behind one fixture module, with the focused 92-test split boundary and six direct MonsterCard cases green. The compact `testing.md` retains current commands, three topic guides own living detail and [testing-evidence.md](testing-evidence.md) owns dated snapshots. Numeric audit passes 5,958/5,958, all 19 goldens pass and full verify passes 78 test files / 770 tests.
- `Done` - Complete [the repository-wide maintainability and dead-code cleanup](maintainability-cleanup.md). Twenty-one unused declarations/helpers, 18 dead rewrite selector families and the unused legacy terminal selector family are removed; active schema/economy public boundaries, dynamic classes, 37 scripts, 40 fixtures and 31 visual baselines have explicit retention evidence. Local Markdown links pass across the final documentation tree. Full verify passes 78 files / 770 tests plus 19 goldens, numeric paths pass 5,958/5,958, functional Chromium passes 78/78 and the CSS-triggered read-only visual rerun passes 20/20 against unchanged baselines.
- `Done` - Complete the bounded [documentation truth cleanup](documentation-truth-cleanup-spec.md). Resolved product/live-integration/Cannon questions, the implemented Worker idea and stale present-tense parity evidence are removed; technical testing owns current commands/results, testing evidence owns dated counts/hashes and operations retains only adopter claim gates. The completed Markdown-only pass adds no product scope, source change, new gate or live-operation claim.
- `Done` - Implement [the MetricList presenter hygiene specification](../technical/metric-list-presenter-hygiene-spec.md). The generic presenter/type now live in `app-presenters.tsx` behind a readonly props-object component API, all eleven consumers use JSX, the fragment preserves the no-wrapper DOM contract and no copy/value/tone/order/CSS path changed. The complete production-preview Chromium gate passes 76/76, and full `npm run verify` passes 624 unit tests, 19 goldens, architecture/type/build/artifact/lint/format/diff gates; only network-disabled dependency audit is skipped.
- `Done` - Implement [the runtime bootstrap controller specification](../technical/runtime-bootstrap-controller-spec.md). A dependency-injected resolver and lifecycle hook now own the async generated-runtime load, saved setup/Duel compatibility and startup PriceSet resolution; `App.tsx` applies one typed result before persistence. Focused suites pass 106/106, startup/recovery browser coverage passes 3/3 and full `npm run verify` passes 635 unit tests plus 19 goldens. Architecture is 70 modules / 57 client-reachable with no cycles or exceptions, and the 880,362-byte generated chunk remains deferred while the 685,731 raw / 197,749 gzip entry stays inside D-094 budgets.
- `Done` - Implement [the local-state recovery controller and Settings panel specification](../technical/local-state-recovery-controller-spec.md). A DOM-free core plus thin React hook now own health/report/failure/block/skip/persist/clear/export mechanics, and the pure panel owns the unchanged Settings recovery DOM. Feature values and the manual-price reset stay caller-owned; focused suites pass 109/109, targeted recovery browser coverage passes 4/4, the complete Chromium gate passes 76/76 and full `npm run verify` passes 645 unit tests plus 19 goldens. Architecture is 73 modules / 60 client-reachable with no cycles or exceptions.
- `Done` - Implement [the Hiscores lookup controller and topbar panel specification](../technical/hiscores-lookup-controller-spec.md). A DOM-free core and thin hook centralize one-shot status loading, normalized-player freshness, latest-request wins, preview/notices and last-player recovery; the typed panel owns the unchanged topbar landmark and disclosure focus interaction while `App` keeps the combat-form Apply bridge. The focused suite passes 19/19, required combined coverage 55/55, targeted Chromium 2/2, complete Chromium 76/76 and full `npm run verify` 664 unit tests plus 19 goldens. Architecture is 76 modules / 63 client-reachable with no cycles or exceptions; API/provider/privacy/schema/CSS contracts are unchanged.
- `Done` - Implement [the rewrite setup file-transfer controller specification](../technical/setup-file-transfer-controller-spec.md), extended by the contextual transfer contract. A DOM-free core and thin hook own bounded setup read/parse, fixed sanitized notices, latest-request-wins review and one-shot consume/Dismiss. New files use dedicated contextual kind/version 1 envelopes; strict storage-shaped version 3 files remain accepted as unknown context and browser storage remains version 3. `App` retains the typed six-state Apply/persistence/Undo block and input reset.
- `Done` - Measure and bound startup according to [the startup/bundle specification](../technical/startup-bundle-performance-spec.md). The repository-owned command captures five paired cold/warm Chromium samples. D-094 defers the required generated-runtime snapshot behind the existing async bootstrap, reducing direct entry JavaScript from 1,562,480 to 683,659 raw bytes and cold FCP median from 112 to 80 ms on the comparison workstation without changing the 240/241 ms app-ready median. D-098 later rebaselines artifact validation to 800,000 raw / 230,000 gzip while retaining that split; total data transfer is intentionally not claimed to shrink.
- `Done` - Implement [the local development startup reliability specification](../technical/dev-startup-reliability-spec.md). The root now has a non-empty pre-React shell, one `starting`/`ready`/`error` DOM contract and a DOM-only failure guard. `npm run dev:checked` reports localhost only after a fresh Chromium navigation reaches the workbench; `npm run test:startup:dev` additionally proves controlled entry failure and direct-versus-transformed price routing against a new strict-port Vite process. Raw `npm run dev` remains browser-independent.
- `Done` - Remove the five legacy-migration adapter-to-app dependency exceptions according to [the implemented layer-refactor specification](../technical/legacy-migration-layer-refactor-spec.md). Rewrite-specific legacy inspection, policy and mapping now live in `src/app/state/legacy-storage-migration.ts`; `src/adapters/storage` retains only generic key/value and versioned persistence mechanics. The 92 focused migration/UI-adapter tests, 3/3 production-preview Import/Keep/Clear cases and full 623-test/19-golden repository gate pass; `npm run architecture:check` reports zero cycles and zero exceptions without changing storage keys, schemas, UI behavior or D-048/D-049 review-only boundaries.

- `Done` - Complete repository handoff hardening according to [handoff-hardening-spec.md](handoff-hardening-spec.md). Root quick start now covers `nvm use`, lockfile-only `npm ci`, the provider-neutral `npm run verify` gate and local run/build paths without `.sources`, credentials, environment variables or a database. The optional LostCityRS/Content checkout is reproducible from its public URL and committed source pin, stale D-066/D-067 docs are corrected and public licensing remains an explicit rights decision. Detached fresh-checkout evidence for commit `43f8b5f` passed install, 523 tests, 19 golden tests, typecheck, build/artifact, lint, format, diff and dependency-audit gates with no raw source checkout.
- `Done` - Deliver the accumulated rewrite worktree as reviewable commits to `origin/master` according to [worktree-delivery-spec.md](worktree-delivery-spec.md). Commits `bc7aa84`, `2ee932d`, `f728cf1` and `c6ac684` preserve the data/runtime, rewrite workflow, parity/visual evidence and documentation boundaries. Typecheck, 497 unit tests, 19 golden tests, build, lint, format, Planner parity, dependency audit and diff checks passed; the managed sandbox blocked only localhost Playwright preview binds with the documented `EPERM` limitation. The push used no force, and local `HEAD` plus `origin/master` matched at `c6ac6845a8458650a6913f3c9d45c09efc8933ff` with a clean worktree immediately after delivery.
- `Done` - Deliver adopter-ready live-integration operations under D-067. The D-066 Cloudflare production adapter, root-path/static routing, headers, exact deploy commands, D-065 no-log configuration and focused tests are complete. D-062-D-064 provide the scheduled market writer, exact root Actions variable and bounded evidence workflow. Account ownership, deployed smoke and observing a concrete cron are adopter runbook steps required for public live/scheduled-current claims, not unfinished repository cards.
- `Done` - Accept the current external sharing model as trusted-tester handoff to one known friend. Do not build full CI/CD, final hosting, public release packaging or deploy-target hardening until a later release decision asks for it.
- `Done` - Decide whether archived legacy `run_sim.py`, `/api/prices`, `/api/scrape` and `/api/hiscores` copy needs compatibility shims or can stay archive-only until legacy deletion. D-044 accepts archive-only for the current rewrite path: do not build legacy compatibility shims unless a later explicit legacy-runtime re-promotion decision changes the boundary.
- `Done` - Decide whether to delete the archived index-sim implementation after the source-backed runtime switch. D-060 retains it as regression, reference and historical evidence outside the supported root runtime. No legacy deletion program is active; reconsider removal only after a later explicit decision and app-owned replacements for any still-needed legacy-executing checks.
- `Done` - Add a small static-run section to operations once the preferred local run command is confirmed. [../operations/README.md](../operations/README.md) now documents `npm run dev`, `npm run build`, `npm run preview` and the archived legacy reference path.
- `Done` - Fix the legacy browser planner `accByType`/stance issues described in `PROJECT_REVIEW_NOTES.md`, or replace the UI path with `src/domain/planner`. The root Planner path now uses `src/domain/planner`; the old planner remains archived reference only.
- `Conditional` - Fix archived legacy current-monster price sync for nested loot entries only if the archived legacy runtime is re-promoted to a supported path. The accepted rewrite market path expands nested/tagged items through `src/data/market-sync-items.ts`; the remaining legacy behavior is legacy-only/archive-only under D-044 unless a future legacy-runtime re-promotion decision changes the boundary.
- `Done` - Decide archived `gamedata.js` duplicate object key hard-gate policy. D-045 accepts no standalone merge-blocking hard gate for duplicate object keys in archived legacy JS; preserve current parsed JavaScript behavior while `gamedata.js` remains an archive/bootstrap source, and resolve canonical duplicate values through generated/source-backed data instead. Raw JSON duplicate keys, duplicate legacy monster ids, malformed loot entries and generated/source-backed duplicate canonical item ids from known alias mappings remain covered by tests.
- `Done` - Close canonical gem/item price alias normalization scope for V1 by D-052 and D-058. Revision 274 cut and uncut gem ids remain distinct source item identities. Economy lookup uses the exact item/price key first and falls back through the reviewed legacy gem alias map only when the exact key is absent; alias-map self-collisions remain hard-gated.
- `Done` - Decide whether ranged/magic/halberd auto-safespot remains default UX. D-046 keeps the current ranged, magic and halberd auto-safespot behavior as the V1 default with visible user override, while leaving source-backed encounter/NPC data free to reopen the default later through a separate evidence-backed decision.
- `Done` - Close dragon halberd missing-size behavior by D-050, then complete the source-backed path under D-071. Generated Revision 274 monsters carry config size with default 1; dragon halberd uses one selected-target hit at size 1 and two above size 1. Missing-size legacy contexts retain the visible D-050 fallback/warning, and adjacent-target simulation remains excluded.

## Rewrite preparation

- `Done` - Implement the accepted v1 replacement scope: combat, result summary, monster compare, loot/economy, trip and planner. Basic combat setup, Result summary, Stats analysis, Dense spreadsheet view, Monster compare, Loot/economy summary, Market price sync, Trip controls, Special attacks and Planner are marked `Valmis` in [../product/feature-inventory.md](../product/feature-inventory.md) for the visible rewrite V1 workflows.
- `Ongoing` - Keep V1 release evidence current after each release-impacting change. [Technical testing](../technical/testing.md) owns the current required commands and latest repository gate summary; [testing evidence](testing-evidence.md) owns dated counts, hashes and superseded failures. D-067 leaves Cloudflare account connection, deployed D-065/Hiscores evidence and concrete market-cron observation to an adopting operator; they are claim gates, not implementation backlog.
- `Done` - Resolve the Playwright smoke release blocker for the accepted current rewrite browser scope. The [superseded failure matrix](testing-evidence.md#superseded-playwright-smoke-failure-matrix) is retained as historical triage evidence; it is no longer the active release-gate state.
- `Done` - Collect the current rewrite release blocker rollup from the partial bucket matrices. [../technical/rewrite-parity-report.md](../technical/rewrite-parity-report.md#current-rewrite-release-blocker-list) now separates the current visible release-required boundary from blocked, decision-needed, later and legacy-only work. No general full-legacy-parity, full visual regression, pane-level browser expansion beyond the completed all-fixture metric strip or exact legacy planner numeric parity requirement is added as a release blocker without a future decision.
- `Done` - Finish the accepted Combat/Stats/Special/Result Summary replacement slice. Visible workflows remain in the rewrite with `SimulationRequest` separate from UI/persisted state. D-071 completes source-backed dragon-halberd/NPC-size behavior; D-050 remains only for missing-size legacy contexts. New special formulas, magic DPS specials and broader browser-display evidence remain later scope.
- `Done` - Prioritize the missing legacy UI workflows listed in [../product/feature-inventory.md](../product/feature-inventory.md) into parity-required, later enhancement, decision-needed, blocked or legacy-only buckets. The core UI/workbench gap snapshot in [../technical/rewrite-parity-report.md](../technical/rewrite-parity-report.md#core-uiworkbench-gap-buckets) now buckets Basic combat setup, Stats analysis, Loot/economy summary, Trip controls, Monster compare and Special attacks without changing their `Valmis` accepted-slice status. Dense/Compare, Combat/Stats/Special/Result, Loot/Economy and Trip slices are closed for the accepted visible V1 replacement scope, and no current open gap in those six rows is `release-required`.
- `Done` - Ring-of-recoil XP attribution decision is closed by D-031: legacy remains comparison evidence, while rewrite combat XP is not reduced by trip-layer recoil damage. The accepted delta is explicit in XP tests and release evidence.
- `Done` - Finish the accepted Loot/Economy V1 replacement slice and D-064 history extension. Current-monster loot actions/settings, value composition/detail, active price context, shared read-only history, local comparisons, movers/trends, local PriceSet override and reset-to-scheduled fallback are implemented. `Save local comparison` and `Clear local history` affect only `index-sim:price-history`; D-049 still keeps full legacy history migration out of V1. Only configured scheduled-run evidence remains conditional operations work.
- `Done` - Implement the [conditional quest/clue loot policy](../technical/conditional-quest-clue-loot-spec.md). The raw parser now retains four reviewed quest-gated rows and 21 clue tertiaries with strict typed eligibility, source chance and sanitized provenance. D-072 keeps them visible but locked to Skip and outside GP, inventory, alch and prayer calculations until an exact player-state contract is separately accepted; blanket inclusion toggles and quest/clue state persistence remain out of scope.
- `Done` - Implement the [conditional-loot presentation follow-up](../technical/conditional-loot-presentation-spec.md). D-089 splits existing view-model rows by typed eligibility and puts inactive quest/clue rows in one native collapsed `Conditional drops` disclosure with source chance, sanitized eligibility and locked Skip. Ordinary loot actions remain in the full decision table; no player state, persistence or calculation path changed. Focused Chromium coverage confirms the disclosure is collapsed by default, the ordinary table excludes the clue row and opening it reveals the truthful inactive state.
- `Done` - Implement [manual item price overrides](../technical/manual-item-price-overrides-spec.md). D-090 adds a capped version-1 `index-sim:manual-price-overrides` overlay after the resolved base PriceSet, exposes Economy apply/reset/confirmed-clear controls and manual item provenance, and integrates the key into local-state health. The cleanup pass keeps drafts item-scoped, resets them on base changes, preserves temporarily unavailable rows as inactive, guards the 512-row limit before schema parsing and keeps the conditional-loot optimizer disabled when no actionable row exists. Base item prices, generated high alch, shared/local history and provider policy remain separate. Focused state coverage passes 48/48 across market and local-state health, the complete Chromium gate passes 76/76 and the read-only numeric audit passes 5,958 cross-path comparisons with zero mismatches. Full `npm run verify` passes 621 unit tests, 19 goldens, typecheck, build/artifact, lint, format and diff checks; the nine-file/two-asset artifact is 1,937,694 bytes with SHA-256 `d40dc6bee4791be7bee37ac8536ca71f76a8c34d2be6e8bff112214d90490a01`.
- `Conditional` - Resolve only explicitly accepted exact-legacy Trip parity questions beyond the accepted food/banking, scarce/AFK target/respawn, reserve summary, prayer restore capacity, general potion carry controls, domain-owned potion recommendation/apply flow, grouped Trip summary and Cannon sparse-linking paths. Prayer potion modeling, live data, canonical data, provider decisions and exact archived legacy `potRec` numerical parity remain outside this backlog item unless separately accepted.
- `Conditional` - Resolve only explicitly accepted remaining Special attacks parity questions after the supported melee/ranged workflow, DBA path, magic unsupported state and D-071 source-backed dragon-halberd behavior. New special formulas, magic DPS specials and adjacent-target simulation remain outside this item unless separately accepted.
- `Done` - Cannon final workbench placement is implemented. The completed all-fixture case covers Cannon through the visible metric strip, while the focused Cannon path covers the Dagannoth dense row and expanded Cannon output. Further pane-level Cannon browser detail is conditional on a later release requirement.
- `Done` - Finish the optional Duel extensions. The Duel pane exports a strict contextual kind/version 1 JSON envelope and imports both it and the prior strict version 1 file through size, JSON, version, context and entity validation. Parsing shows an in-memory setup/add/update/limit preview; explicit Merge preserves unrelated snapshots, updates matching ids and adds only to the 12-entry cap. Computed-result payloads retain sanitized rejection, browser storage remains version 1 and compatible legacy `sim_input_v3.duelSetups` migration remains complete. The on-demand cross-monster matrix retains its existing comparison and explicit lifecycle behavior. Account-backed saves and public collection discovery remain outside scope.
- `Done` - Add the bounded Duel setup diff and calculated-impact review. Saved rows now expose grouped normalized active setup differences, visible DPS and kills/hr deltas and an expandable ten-metric snapshot-minus-live impact summary. Shared target/cannon/loot/price context, Planner targets and inactive per-style caches remain excluded, the snapshot schema stays version 1 and D-068 rejects causal attribution claims.
- `Done` - Add Stats special-attack and cannon source distributions. Supported specials show a per-special-hit histogram and active non-idle cannon shows a per-fired-cannonball histogram through the existing domain bucket model. Inactive/invalid sources remain empty, D-069 excludes aggregate multi-hit/kill/trip/hour distributions and no persisted or request state changes.
- `Done` - Implement the bounded [whole-loadout optimizer](../technical/bounded-loadout-optimizer-spec.md). D-073 searches only the active style's visible current-revision weapon and equipment choices, evaluates a capped deterministic frontier through the accepted current-target normal-DPS simulator, never lowers the baseline and supports one complete Undo. Existing hidden-tier, two-handed and non-blocking requirement-warning policies remain intact; price-, quest-, requirement- and future-gear-aware optimization stays later scope.
- `Done` - Add the [numeric requirement-aware optimizer follow-up](../technical/requirement-aware-loadout-optimizer-spec.md). D-088 makes current-level eligibility the checked-by-default candidate policy, filters only new optimizer weapon/equipment choices through the existing generated-first/D-051-fallback lookup, reports skipped choices and retains explicit warning-only D-073 behavior. The unchanged current setup remains the no-regression baseline, manual selection stays non-blocking and no request, persistence, share, Duel or Planner schema changes. Focused view-model coverage passes 93/93 and the production-preview eligibility/apply/Undo case passes 1/1. Full `npm run verify` passes 616 unit tests, 19 goldens, typecheck, build/artifact, lint, format and diff checks; the nine-file/two-asset artifact is 1,930,006 bytes with SHA-256 `94cab4408737ec0f797e8051cf69f5bffe68f746bb97a83334b9b91bcb35fe6e`. Price-, quest-, ownership- and future-gear-aware optimization remains later scope.
- `Done` - Complete the bounded workbench keyboard pass. The workbench now uses a roving tablist/tabpanel contract with arrow/Home/End activation, exposes a first-focus skip link, gives interactive controls a high-contrast focus-visible outline and reduces Dense rows to one Tab stop with arrow/Home/End movement plus Enter/Space selection. The full production-preview browser suite passes 58/58 under D-070; this is not a WCAG certification claim.
- `Done` - Add richer Economy trend analysis and D-064 shared-history extension. Read-only committed history plus capped local comparisons drive movers, sparklines, item trends and exact points. Missing values/zero baselines remain finite, local storage stays version 1 and no database or backend-managed mutable history is introduced.
- `Done` - Monster Compare / Dense Spreadsheet V1 replacement slice is closed. The visible dense all-monster table, sort/filter, row target selection, active/forced-visible target marker, per-monster markers, persisted rewrite-owned relevance state, visible-row scale indicators, responsive overflow, representative browser numeric evidence and completed optional all-fixture metric-strip expansion are accepted for V1 replacement. After measured heavy calculations missed the documented budget, Dense Compare, Planner and on-demand Duel matrix execution moved behind the typed cancellable Web Worker boundary with Chromium Long Task evidence. D-032 remains the release-path numeric classification; the repository-local visual suite is also implemented. Pane-level detail beyond the covered states, deeper legacy compare-state migration, final Dense-vs-workbench default UX and promotion to a canonical remote visual gate remain later or decision-needed scope.
- `Done` - Complete the 2026-07-11 rewrite reliability audit. Setup/Duel/current-game-data recovery, duplicate JSON identities, browser response and local-state size limits, generated schema key consistency, live report chronology/source mapping, bounded ephemeral rate-limit maps, provider status timeouts, scheduled-only market startup ownership and non-finite combat/display guards now fail closed with sanitized user-visible fallback behavior. Focused unit/browser evidence and the full repository gates own the regression line; no auth, database, tenant, provider, future gear, quest-state, deploy or `SimulationRequest` boundary changed.
- `Done` - Complete the optional all-fixture browser-display expansion. The rewrite-owned fixture adapter maps all 18 golden cases into validated state and the production-preview matrix compares visible metrics with the source-backed view model. The run also closed dynamic PriceSet composition: scheduled market prices retain generated price fallbacks and generated high alch remains authoritative.
- `Done` - Finish the accepted Planner replacement slice. The workbench workflow, versioned UI state, domain adapter, deterministic goldens, current-revision gear-pool allowlist, chart/timeline output and `sim_planner_v1` review-only boundary are accepted. D-071 completes raw source-backed numeric requirement mapping, while D-047, D-048 and D-051 retain future-gear, migration and missing-row fallback policies. Quest-state requirements, fallback removal and exact legacy numeric parity remain later work.
- `Done` - Implement the bounded [legacy Planner behavior parity and gap audit](../technical/legacy-planner-parity-spec.md). The fixed-source test-only VM adapter, 16-case matrix, 32 reference-context/current-product comparisons, normalized field diffs, explicit baseline update path and deterministic [current report](planner-parity/current.md) are implemented. The classified baseline records 29 accepted rewrite deltas and three Revision 274 source-data deltas, with zero rewrite gaps or review blockers. Planner stays `Valmis`; D-047 future/hypothetical gear, D-048 `sim_planner_v1` migration, D-051 requirement fallback removal, providers and known legacy defects remain outside this card.
- `Ongoing` - Extend mocked live integration fixtures as upstream contracts evolve. D-061 has sanitized Hiscores provider/schema/security coverage, and D-062 has sanitized item-page/estimator coverage plus separately recorded opt-in live dry-run evidence; default tests never call live upstreams. Promote repository-variable and scheduled-run evidence only through the owning operations specification.
- `Done` - Close legacy live-integration storage migration scope by D-049/D-063. Compatible hiscores last-player and market prices import; legacy alch overrides are validated then replaced by generated values. Legacy scrape metadata resets only on confirmed Clear and full legacy history remains review-only/not migrated. Backend/account history or full legacy history import needs a new decision.
- `Done` - Replace the legacy runtime snapshot adapter with the authoritative current `GameDataSnapshot`. `src/adapters/generated` builds the root context from Revision 274 data, scheduled market prices, generated price fallbacks and authoritative generated alch. Runtime readiness has zero blocking coverage gaps; the static bridge remains regression/reference evidence.
- `Done` - Implement the accepted game revision bump workflow. The raw generator resolves every expected identity, extracts 63/63 loot tables with 1,126 top-level rows including D-072 conditional loot, 63/63 NPC sizes and 94 item requirement rows, requires explicit `--game-revision`, writes one agreeing committed source pin/snapshot/report and enforces path/output hygiene. The current regenerated report passes 11/11 representative cases under D-055/D-057/D-071/D-072 and finds 38 informational outliers with the configured first 25 shown; runtime readiness has zero blockers. D-059 owns root consumption and D-060 retains legacy evidence.
- `Done` - Complete repository readiness for the D-062 scheduled market writer, extended by D-085. The repo has the first-page `/items/{slug}` Inertia adapter, sequential 350 ms allowlisted collection, coin-only buy/sell observation handling with ambiguous item offers skipped, adaptive MAD/median filtering, 90/30-day freshness gates, prior-value retention, 90-day 12-hour history plus older daily compaction, generated alch ownership and atomic validation plus a GitHub Actions allowlist for the three-file `prices.json`/`price-provenance.json`/`price-history.json` logical set. Bounded shape evidence, public crawler-policy review and the exact root `MARKET_PRICES_UPSTREAM_URL` variable are evidenced. Under D-053/D-067, a future operator validates a configured run for the current allowlist before making a scheduled-current claim.
- `Done` - Replace the browser raw-legacy-data bootstrap with a committed source-backed snapshot. The root app imports `src/adapters/generated`; no normal browser bootstrap executes archived legacy sources or loads the legacy-derived bridge. The static bridge writer/readiness checks remain available for regression/reference evidence. Bundle-size reduction and legacy source deletion stay conditional under the separate deletion decision.
- `Done` - Fold combat/equipment, trip/loot/supply and economy slices into the UI-facing result contract. The combat-only result is explicitly named `CombatSimulationResult`, `src/domain/simulation` defines the composed `FullSimulationResult` foundation with scoped warnings and current XP/rate assembly, the primary `createSimulationViewModel()` path exposes and consumes that composed result, and Dense Compare/Compare plus Duel row generation now read their rate, XP and economy numbers from the same composed result path. Planner remains intentionally combat-focused with explicit `CombatSimulationResult` typing. Whether a later public contract should be renamed back to `SimulationResult` remains an open naming decision, not remaining implementation debt for this backlog row.
- `Conditional` - Extend the rewrite UI to full legacy parity only if a later release explicitly raises the bar beyond the accepted selected parity slice. The current V1 replacement scope is already accepted for the visible workflows tracked in feature inventory.
- `Done` - Implement Legacy Migration V1 custom setup/cannon and related compatible state import. The flow detects every known key, shows metadata-only outcomes, imports safe setup/custom/cannon/Duel/hiscores/market-price/UI preference data, keeps current rewrite entries on conflicts and reports invalid or ambiguous values safely. Legacy market alch maps cannot override generated values. D-048 keeps Planner and D-049 keeps full legacy history review-only/not migrated; confirmed Clear removes only the displayed known-key allowlist.
- `Done` - Finish the generated planner/loadout requirement consumer and raw-source phase. `GameDataSnapshot` has a strict Attack/Strength/Defence/Ranged/Magic requirement contract. Normalized fixtures test merge/conflict behavior, while the active raw generator parses reviewed `levelrequire` RuneScript families, multi-line quest wrappers and Iban's fixed gate, failing unknown or conflicting shapes. Planner filtering/unlocks and loadout/quick-action warnings consume 94 generated rows before D-051 fallback; quest completion state remains excluded.
- `Done` - Close manual `ITEM_REQUIREMENTS` fallback scope by D-051. Generated D-071 requirements are primary and the fallback remains only for legacy or missing rows. Removing it later requires an explicit coverage and quest-state decision; it must not reopen D-047 future gear, import `sim_planner_v1` or imply legacy numeric parity.
- `Done` - Close future/hypothetical planner gear scope for V1 by D-047. Planner V1 remains current-revision-only and uses source-backed or snapshot-backed non-hypothetical gear from the active `GameDataSnapshot` and accepted default planner pool. Future or hypothetical gear requires a separate later product/data provenance decision before any UI exposure, planner pool change or tests are added.
- `Done` - Finish market price repository acceptance and provide the [adopter live-evidence runbook](../operations/market-live-evidence-spec.md). Visible scheduled/imported prices, generated alch, shared/local history, catalog-audited mapping, coin-only offer parsing, the full live dry-run and exact root repository variable are implemented/evidenced. Public copy for any future instance must not claim scheduled-current prices until its configured scheduled run and any resulting market commit are validated.
- `Done` - Close planner `localStorage` migration scope for V1 by D-048. Legacy `sim_planner_v1` stays detect/review-only and is not parsed or imported into rewrite-owned `index-sim:planner-ui`; Import and Keep preserve the legacy key, and confirmed Clear may remove it with the other known legacy keys. Any future planner-state import requires a separate explicit product/storage decision.
- `Done` - Finish the adopter-ready Cloudflare deployment package according to [../operations/public-deployment-spec.md](../operations/public-deployment-spec.md). D-066 accepts root-path Workers + Static Assets, enabled Hiscores and automatic validated `master` deploys. The repo implements the production Worker, exact Wrangler commands, Worker-first API routing, SPA fallback, `_headers`, disabled observability/Logpush, artifact validation, focused tests and operator checklists. D-067 leaves account connection, preview/production verification, market-commit propagation and rollback rehearsal to whoever operates a public instance.
- `Conditional` - Delete archived legacy runtime files only after full parity, authoritative generated runtime replacement and legacy-deletion decisions are accepted. The root app no longer executes legacy source files during normal bootstrap, but `gamedata.js`, `engine.js` and `equipment.js` still feed golden/reference comparison and the legacy-derived static bridge regeneration path, so deletion remains later replacement-readiness work, not a current handoff blocker.

## Specced next extensions

- `Done` - Finish [user-facing language, units and information hierarchy](../technical/user-facing-language-units-information-hierarchy-spec.md) without reopening `Valmis` feature scope. Source-backed item/monster names are primary, technical ids live in labelled native disclosures, tick/second and GP/XP semantics plus dense abbreviation expansions share one accessible presentation contract, and Settings keeps only a short active-PriceSet summary with a focus-correct Economy action. Economy retains all detailed Market, PriceSet, price-note and history ownership; calculations, generated data, persistence and PriceSet transactions remain unchanged.
- `Done` - Implement the high-priority, L-sized [Workspace backup and restore](../technical/workspace-backup-restore-spec.md) workflow for all nine user-owned local-state areas. The bounded registry/envelope, safe export, zero-mutation review, typed Replace/Merge plan, selected-only logical atomic Apply with exact handled-failure rollback, explicit session-only path, recovery integration and complete durable/session-only Undo are implemented. The last Hiscores player remains a two-sided privacy opt-in, legacy-migration dismissal remains recipient-local and exhaustive policy fails when a future local-state id is unclassified. This is local file durability, not crash-atomic, account, database, server or cross-device synchronization work.
- `Done` - Implement [contextual item-price correction](../technical/contextual-item-price-correction-spec.md). Stable structured notice actions now carry the exact `itemId` from Result, row/nested Loot and Economy through one App-owned latest-request-wins transition. Correctable items activate Economy, preselect the existing D-090 editor and focus `Manual price`; inspect-only targets open and focus their exact Economy notice. A single actionable Result issue may use the direct action, while multiple issues retain D-101's compact `Review price data` path. Apply/Reset identify the item/value and whether current results use manual or base data. Manual storage, warning relevance, formulas, history, generated high alch and D-102's advanced full-PriceSet tools remain unchanged.
- `Done` - Implement source-backed NPC attack profiles and shared Trip/Risk incoming-damage ownership according to [the implemented specification](../technical/source-backed-incoming-damage-spec.md) and D-081. The deterministic [Revision 274 NPC attack source audit](npc-attack-source-audit.md) classifies all 63 active monsters as 55 exact and eight partial paths. Generated profiles retain formula ids, sanitized inputs, resolved max hit and repository-relative provenance; Trip owns one normalized descriptor, Risk samples exact profiles from it, and Trip/Stats/Risk expose partial and compatibility models as mean-only. The four explicit dragon weights remain partial with dragonfire separate, and no contextual weights were invented. Player HP/death, positioning, persistence, backend and provider scope remain excluded.
- `Done` - Implement modeled risk and variability according to [the accepted specification](../technical/risk-variability-spec.md). The new Risk tab provides deterministic seeded kill-time P10/P50/P90, food-sufficiency risk, kills/trip and trip-cycle ranges, timed net-GP ranges plus drop/GP target probabilities through the cancellable worker boundary. `FullSimulationResult` remains unchanged expected-value truth; source-matching/stale state, mean-only coverage warnings and compact TTK/net-GP/Trip ranges are visible, with no persistence, account, database or provider scope. The repository gate passed 586 unit tests, 19 goldens, typecheck, build/artifact, lint, format and diff checks. The focused production-preview Playwright case passes 1/1 in Chromium and covers Run, all five outputs, coverage/warnings, fresh/stale state and cancellation; its runtime pass also closed the status-priority race where stale prior results could hide `Cancelled`.
- `Done` - Implement static shareable setup permalinks according to [../technical/shareable-setup-permalink-spec.md](../technical/shareable-setup-permalink-spec.md), extended by the contextual transfer contract. New bounded v2 fragments carry validated revision plus snapshot id alongside active form and current-monster Cannon/loot choices; v1 remains readable with exact-id or unknown-revision normalization. Both versions exclude PriceSet/history/collections/player name/computed output, remove the captured fragment, review before Load, support Dismiss and one complete in-memory Undo, and keep clipboard failure non-fatal. Server short links, account-backed saves, public discovery and shared Duel/Planner collections remain outside this completed card.
- `Done` - Implement the repository-local Chromium visual regression suite according to [../technical/visual-regression-spec.md](../technical/visual-regression-spec.md). `playwright.visual.config.ts` owns a visual-only Chromium project, deterministic fixture state, fixed browser time, desktop/tablet/mobile pane coverage, Darwin-reviewed platform baselines and explicit compare/update commands. The functional config ignores `*.visual.spec.ts`, so the normal functional suite is not duplicated. The implementation also removed the mobile 72vh active-pane overlap exposed during baseline review. This card does not choose a CI provider or make screenshots a remote merge check; that promotion remains a separate decision.

## Documentation maintenance

- `Ongoing` - Update this backlog when an item moves into active work or becomes obsolete.
- `Ongoing` - Move larger speculative ideas to [idea-inbox.md](idea-inbox.md). Remove stale ideas from the inbox when they become accepted backlog/decision work or are already covered by completed feature inventory.
- `Ongoing` - Consolidate changing test counts, artifact sizes and hashes out of the living testing topic guides into [testing evidence](testing-evidence.md). [Technical testing](../technical/testing.md) and its topic guides should own current commands, coverage boundaries and interpretation; dated run results should have one historical owner so they do not drift across multiple living documents.
