# Decisions

Use this log for choices that future agents must not re-litigate accidentally.

## Accepted

| ID    | Date       | Decision                                                                                                                                                                                                                              | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | 2026-07-05 | Use `AGENTS.md` plus `docs/` as the AI-agent memory layer.                                                                                                                                                                            | The repo needed a stable map for future bounded work and rewrite planning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-002 | 2026-07-05 | This documentation pass does not rewrite application code.                                                                                                                                                                            | The requested deliverable is documentation that enables a safer rewrite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-003 | 2026-07-05 | `docs/technical/rewrite-spec.md` owns the implementation-grade rewrite specification.                                                                                                                                                 | Future rewrite work needs one target contract instead of scattered recommendations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-004 | 2026-07-05 | Use npm for the initial rewrite scaffold.                                                                                                                                                                                             | The rewrite spec allowed npm as the lowest-assumption default when no human package-manager preference was given.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-005 | 2026-07-05 | Golden fixtures preserve current `SimEngine.simulate()` behavior without pre-fixing known bugs.                                                                                                                                       | The rewrite needs a reproducible current baseline before intentional deltas are accepted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-006 | 2026-07-05 | Node-based npm tool scripts call npm's `$NODE` explicitly.                                                                                                                                                                            | The local PATH can expose an older parent `node` binary before NVM, while Vite 7 and ESLint 10 require a newer Node runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-007 | 2026-07-05 | The first domain extraction slice covers combat/equipment only.                                                                                                                                                                       | Keeping trip, loot, economy, planner and persistence out of this slice keeps parity testable and avoids hidden behavior rewrites.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-008 | 2026-07-05 | The first data/economy slice validates legacy data and prices without adding live sync.                                                                                                                                               | Static-first `PriceSet` and snapshot validation remove global mutation risk while keeping backend and refresh workflow decisions open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-009 | 2026-07-05 | Trip, loot and supply rewrite rules are owned by pure `src/domain/trip`.                                                                                                                                                              | This unifies previous `trip.js`, `engine.js` and `gamedata.js` rule paths behind explicit `GameDataSnapshot` and `PriceSet` inputs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-010 | 2026-07-05 | Planner rewrite search is owned by pure `src/domain/planner` and consumes domain APIs.                                                                                                                                                | The planner must not duplicate hit chance, max hit, equipment bonus or stance truth; future gear remains outside canonical data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-011 | 2026-07-05 | The first rewrite UI stores only `index-sim:rewrite-setup` version 1 state.                                                                                                                                                           | No human decision exists to migrate or reset legacy `sim_input_v3`, `sim_planner_v1` or legacy price keys, so this slice leaves them untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-012 | 2026-07-05 | Root `index.html` is the Vite rewrite entrypoint; legacy CDN/Babel HTML is archived.                                                                                                                                                  | This removes CDN React, runtime Babel and real script-order `window.*` loading from the production app path while preserving legacy parity evidence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-013 | 2026-07-05 | CSP and static security headers are documented as deploy requirements, not hard-coded yet.                                                                                                                                            | No deploy target exists, and dev-time Vite/Playwright flows should not be constrained by an untested host-specific CSP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-014 | 2026-07-05 | The next UI parity target is the legacy-style dense combat spreadsheet view.                                                                                                                                                          | Existing users need the compact all-monster table workflow before the rewrite can replace the legacy UI ergonomically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-015 | 2026-07-05 | Hiscores lookup and market price refresh are product features with a repo-owned integration spec.                                                                                                                                     | The original app exposed these workflows, and the accepted direction is to implement them properly instead of keeping them local-dev-only or removing them. Market refresh was later narrowed to scheduled static JSON by D-033.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-016 | 2026-07-06 | Rewrite setup version 2 stores per-monster cannon settings and does not implicitly migrate v1.                                                                                                                                        | Cannon settings add a new persisted rewrite-owned state branch; silent migration is avoided until a broader setup migration/reset policy is accepted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-017 | 2026-07-06 | Treat the rewrite as a new implementation that preserves end-user workflows, not legacy internals.                                                                                                                                    | The current product is not production-bound, and old architecture or logic should not be kept when better LostCityRS-backed behavior is available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-018 | 2026-07-06 | Use LostCityRS/Content Revision 274 as the primary game-content source when it can be verified.                                                                                                                                       | The rewrite needs a stronger content source than the legacy bundled data, while still allowing documented gaps when the upstream repo is incomplete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-019 | 2026-07-06 | Generate a validated `GameDataSnapshot` as the app-owned content contract.                                                                                                                                                            | Domain modules need one explicit, typed snapshot instead of reading content directly from legacy globals or raw upstream files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-020 | 2026-07-06 | Keep the target architecture static-first without locking the hosting provider yet.                                                                                                                                                   | GitHub Pages, Netlify or another static-capable host should remain possible until hiscores API/hosting constraints are known.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-021 | 2026-07-06 | Use `markets.lostcity.rs` as the target market price source and keep 12-hour price history snapshots.                                                                                                                                 | The accepted price direction is small enough for retained history, but the exact automation and deploy workflow remain separate implementation details.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-022 | 2026-07-06 | Hiscores is a v1 product requirement, but the authoritative API and hosting model remain open.                                                                                                                                        | The visible hiscores HTML endpoint exists, but a possible API answer is pending and the final direct-fetch vs proxy decision should not be guessed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-023 | 2026-07-06 | Build the v1 `GameDataSnapshot` in staged scope: required calculation data first, extensible format.                                                                                                                                  | This keeps the rewrite moving without forcing every LostCityRS content area into the first data-generation pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-024 | 2026-07-06 | Surface uncertain, missing or approximated values as lightweight UI info/warning markers.                                                                                                                                             | Users should see calculation confidence without the normal UI becoming dominated by warning copy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-025 | 2026-07-06 | V1 replacement scope is combat, result summary, monster compare, loot/economy, trip and planner.                                                                                                                                      | These are the current end-user workflows the rewrite must preserve before it can replace the existing app.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-026 | 2026-07-06 | Start with main-thread calculation plus memoization/debounce behind a worker-compatible runner.                                                                                                                                       | This is simpler for v1 while preserving a clean upgrade path to Web Workers for compare/planner workloads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-027 | 2026-07-06 | Use a measurable v1 performance budget: normal input updates around 100 ms and no long UI blocks over about 200 ms.                                                                                                                   | The rewrite must catch the level-input jank already observed in the new UI path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-028 | 2026-07-06 | Use legacy golden parity as regression evidence, but allow documented LostCityRS-backed intentional deltas.                                                                                                                           | The old app can help find accidental changes, but it is not the final truth source for a full rewrite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-029 | 2026-07-06 | The rewrite can replace the current app only after v1 features, parity/golden tests, performance budget and security checks pass.                                                                                                     | Replacement needs a balanced acceptance gate without turning the rewrite into an endless full-edge-case proof project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-030 | 2026-07-06 | Legacy storage migration starts as non-writing detection plus a safe setup report, not silent auto-migration.                                                                                                                         | The rewrite can explain old local state later without deleting legacy keys or importing unvalidated planner, price, custom setup, compare, cannon or hidden-tier data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-031 | 2026-07-06 | Ring-of-recoil XP attribution is an accepted rewrite intentional delta from legacy.                                                                                                                                                   | Legacy remains useful as a comparison value, but the rewrite may calculate this path anew: recoil is modeled in the trip layer for kill speed, capacity and supply cost, while combat XP stays attributed to direct player combat damage instead of being reduced by trip-layer recoil damage.                                                                                                                                                                                                                                                                                                                                                        |
| D-032 | 2026-07-07 | Dense/Compare release-path browser numeric coverage is sufficient for the current release slice.                                                                                                                                      | The current evidence covers the full all-monster dense table workflow, sorting/filtering/target selection, active-row and marker states, mobile/tablet overflow, visible-row scales and representative browser-rendered numeric snapshots. All-fixture browser-display expansion and full visual regression are later/decision-needed evidence, not release blockers for this slice.                                                                                                                                                                                                                                                                  |
| D-033 | 2026-07-08 | Market price refresh writes scheduled static JSON snapshots, not user-triggered upstream sync or a database.                                                                                                                          | Fresh shared prices should not let users or runtime traffic create extra upstream load. Scheduled repo automation can fetch `markets.lostcity.rs`, update `prices.json` and `price-history.json`, validate them and commit only real diffs. D-063 later moved high-alch authority to generated game data and removed `alch.json` from the scheduled writer output set.                                                                                                                                                                                                                                                                                |
| D-034 | 2026-07-08 | Run the scheduled market price writer with GitHub Actions cron and same-repo commits.                                                                                                                                                 | The expected 2x/day Linux runner usage fits normal free GitHub Actions allowances, avoids a new server, keeps secrets out of the browser app and leaves every accepted price change in git history.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-035 | 2026-07-08 | Treat game revision bumps as reviewed development changes, not scheduled data refreshes.                                                                                                                                              | Game revisions can change data, formulas and expected simulation results. The app should track the latest accepted revision, not the latest available revision, and only change it through a reviewed PR with generated-data diffs, validation and calculation-impact evidence.                                                                                                                                                                                                                                                                                                                                                                       |
| D-036 | 2026-07-08 | Use a manual repo-owned `npm run data:generate` workflow for generated game data.                                                                                                                                                     | The generator should read a pinned local LostCityRS/Content checkout, write a validated committed normalized current `GameDataSnapshot` plus calculation-impact report, and be run in a normal development PR rather than by schedule, browser action or automatic upstream tracking.                                                                                                                                                                                                                                                                                                                                                                 |
| D-037 | 2026-07-08 | Commit only the current normalized game-data snapshot, not raw upstream data or snapshot history.                                                                                                                                     | The local LostCityRS/Content checkout path is `.sources/lostcity-content/` and stays gitignored. The generator overwrites `src/data/generated/source-pin.json`, the purpose-built runtime snapshot `src/data/generated/game-data.json` and `docs/project/revision-impact/current.md`; older snapshots and reports are kept only through git history.                                                                                                                                                                                                                                                                                                  |
| D-038 | 2026-07-08 | Limit generated `GameDataSnapshot` v1 to simulator-consumed domain data.                                                                                                                                                              | The snapshot is runtime input for combat, trip, loot, economy warnings, planner and loadout requirement checks, not a mirror of LostCityRS/Content. Include only normalized monsters, items, equipment/weapons/ammo/spells, drop rows, planner/loadout requirements and provenance/warnings needed by accepted app workflows.                                                                                                                                                                                                                                                                                                                         |
| D-039 | 2026-07-08 | Gate game revision PRs on a committed revision-impact report.                                                                                                                                                                         | `docs/project/revision-impact/current.md` must identify old/new source refs, generator version/time, generated data changes, requirement/provenance changes, representative numeric diffs, intentional deltas and validation results. A revision bump cannot merge until changed calculation outputs are accepted as intentional or fixed in code/tests.                                                                                                                                                                                                                                                                                              |
| D-040 | 2026-07-08 | Use a hybrid calculation-impact suite for game revision reports.                                                                                                                                                                      | The merge-blocking part is a small representative suite seeded from current combat/trip/economy golden fixtures and planner golden fixtures, extended with focused generated-data cases for cannon, recoil, alch, loot-heavy, high-defence and low-level coverage when missing. A broader all-monster scan is reported for visibility but is informational unless a human promotes a finding to a blocker.                                                                                                                                                                                                                                            |
| D-041 | 2026-07-08 | Use fixed simple melee, ranged and magic baselines plus percentage thresholds for the informational all-monster scan.                                                                                                                 | The scan is a review aid, not an optimizer: one explicit baseline per combat style, no best-in-slot search and no gear progression. It reports notable outliers when DPS, kills/hr or XP/hr changes exceed 10%, GP/hr or GP/XP changes exceed 25%, required combat/drop/economy data is missing, warning count increases or monsters enter/leave the scan.                                                                                                                                                                                                                                                                                            |
| D-042 | 2026-07-08 | Legacy Migration V1 imports compatible nested custom setup and cannon maps, while planner state and full legacy price history stay review-only.                                                                                       | Users should not be left in a found-but-not-imported state for reusable per-monster setup and cannon data that already has rewrite-owned targets. D-048 later keeps `sim_planner_v1` review-only/not migrated for V1, and D-049 later keeps full legacy price history review-only/not migrated for V1. Implementation must preserve metadata-only review, no inspection-time writes, Import keeping legacy data and confirmed Clear limited to known legacy keys.                                                                                                                                                                                     |
| D-043 | 2026-07-08 | Use a trusted-tester handoff model before any public release pipeline.                                                                                                                                                                | The immediate goal is to give the app to one known friend for functional testing. Full CI/CD, final hosting, public release process and deploy-specific hardening are deferred until a broader release is accepted.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-044 | 2026-07-09 | Keep archived legacy `run_sim.py`, `/api/prices`, `/api/scrape` and legacy `/api/hiscores` behavior archive-only; do not build compatibility shims for the current rewrite path.                                                      | The root Vite rewrite already owns the production app path and uses service-aware live-integration boundaries plus scheduled/static market data. Rebuilding legacy shims would add backend/provider/security surface without current user value. Archived legacy files may keep the old copy as evidence, but any future shim requires an explicit legacy-runtime re-promotion decision.                                                                                                                                                                                                                                                              |
| D-045 | 2026-07-09 | Do not add a standalone merge-blocking hard gate for duplicate object keys in archived `gamedata.js`; resolve canonical duplicate values through generated/source-backed data instead.                                                | `gamedata.js` is archived legacy data and JavaScript's last-value-wins behavior is part of the current golden baseline. Choosing first vs last vs source-backed values inside legacy JS would silently promote the archive back into a truth source. Existing gates already cover duplicate JSON keys, duplicate legacy monster ids, malformed loot entries and generated/source-backed duplicate canonical item ids; remaining duplicate-value cleanup belongs to the authoritative generated-data workflow and runtime snapshot replacement.                                                                                                        |
| D-046 | 2026-07-09 | Keep the current ranged, magic and halberd auto-safespot behavior as the V1 default UX, with explicit user override, until source-backed encounter data justifies a change.                                                           | Current fixtures and V1 browser evidence already cover ranged/magic/halberd safespot-style paths, and Trip exposes safespot assumptions as visible/reviewable state. Changing the default now would alter user-visible rates without stronger source-backed encounter data. This is a V1 UX/default decision, not a final combat truth claim; source-backed NPC/encounter data can reopen the default later through a separate evidence-backed decision.                                                                                                                                                                                              |
| D-047 | 2026-07-09 | Keep future/hypothetical planner gear out of V1. Planner V1 gear pools stay current-revision-only, source-backed or snapshot-backed, and non-hypothetical.                                                                            | The accepted Planner V1 workflow already offers only items present in the active `GameDataSnapshot` and default planner pool. Adding future gear would require a separate product/data provenance model, user-facing labels and tests without improving the current release path. Future or hypothetical gear can be reopened only through an explicit product/data provenance decision.                                                                                                                                                                                                                                                              |
| D-048 | 2026-07-09 | Keep legacy `sim_planner_v1` state review-only/not migrated for V1.                                                                                                                                                                   | The rewrite Planner has its own versioned `index-sim:planner-ui` state contract and accepted replacement line. Parsing raw legacy planner payloads could overwrite rewrite-owned Planner state, silently reinterpret old assumptions and pull V1 toward legacy-clone behavior. Import and Keep preserve the legacy key; confirmed Clear may remove it with the other known legacy keys. Any future planner-state import requires a separate explicit product/storage decision.                                                                                                                                                                        |
| D-049 | 2026-07-09 | Keep full legacy price-history payloads review-only/not migrated for V1.                                                                                                                                                              | Compatible legacy market prices import, but historical payload migration would require separate validation/retention semantics. D-063 later replaces legacy alch overrides with generated values, and D-064 adds read-only committed shared history without changing this legacy migration boundary. Import/Keep preserve legacy history keys; confirmed Clear may remove them with other known legacy keys.                                                                                                                                                                                                                                          |
| D-050 | 2026-07-09 | Keep the current dragon halberd NPC-size fallback with a visible warning for V1; do not add manual NPC-size data.                                                                                                                     | The rewrite already surfaces the missing NPC-size assumption near dragon halberd special metrics. Adding hand-maintained NPC-size data now would create a new data truth source and could change formula baselines without generated/source-backed evidence. Source-backed NPC-size data can reopen the behavior later through generated-data evidence and an explicit formula/baseline decision.                                                                                                                                                                                                                                                     |
| D-051 | 2026-07-09 | Keep manual `ITEM_REQUIREMENTS` as a legacy/missing-generated-data fallback for V1.                                                                                                                                                   | Generated requirements remain the primary source when `GameDataSnapshot.requirements` is present, but the fallback protects legacy and missing-generated-data snapshots from losing useful non-blocking requirement warnings. Removing or demoting it requires accepted authoritative upstream requirement mapping, broader real-source coverage and generated requirement impact evidence. This decision does not decide quest requirements, legacy planner numeric parity or the runtime generated snapshot switch.                                                                                                                                 |
| D-052 | 2026-07-09 | Keep the current deterministic legacy-derived canonical item/price alias resolver for V1; defer full source-backed canonical key normalization.                                                                                       | The resolver already covers the known gem aliases, canonical-first price lookup, fallback warnings and duplicate alias collision tests without pretending archived legacy data is authoritative. Full source-backed normalization still belongs to the generated/source-data workflow, where authoritative ids, aliases and duplicate-value choices can be reviewed with generated evidence.                                                                                                                                                                                                                                                          |
| D-053 | 2026-07-09 | Do not require verified live scheduled market price workflow evidence for V1 or the trusted-tester handoff; require it only before claiming scheduled-current market prices.                                                          | The accepted visible V1 slice can use committed static prices, bundled fallback and local import/override with clear freshness copy. Live response/catalog verification, a full dry-run and exact `MARKET_PRICES_UPSTREAM_URL` configuration were completed; the first successful configured scheduled run still depends on external evidence and does not block the current handoff. It remains mandatory before release notes, UI copy or docs claim automatically current scheduled market prices.                                                                                                                                                 |
| D-054 | 2026-07-09 | Keep legacy-derived runtime data as the V1/trusted-tester runtime truth; defer source-backed generated `GameDataSnapshot` runtime bootstrap.                                                                                          | This was the accepted boundary while raw source coverage and impact evidence were incomplete. D-059 supersedes the runtime-truth portion after those criteria closed. The legacy-derived snapshots remain reference/regression evidence; bundle-size reduction and legacy deletion still require separate work and decisions.                                                                                                                                                                                                                                                                                                                         |
| D-055 | 2026-07-09 | Accept explicit LostCityRS/Content Revision 274 combat values and `tribesman` `random(138)` loot weighting as source-backed candidate deltas; keep quest-gated and clue-scroll tertiary drops outside the current core-loot snapshot. | D-018 makes Revision 274 the primary verifiable content source and D-028 allows documented source-backed deltas from legacy. Raw-source impact evidence shows two representative magic-DPS deltas caused by explicit upstream magic levels and one loot-only threshold delta caused by the explicit `random(138)` roll, while the remaining representative rows stay within thresholds. Four quest-gated drops and 21 clue-scroll tertiary rows require separate quest/clue policy and remain explicit reported exclusions. This accepts candidate data interpretation only; it does not switch the committed generated snapshot or root runtime.     |
| D-056 | 2026-07-09 | Accept Revision 274 equipment bonuses and source-cost-derived alch values as generated candidate truth, including Unholy symbol +2 attack bonuses and Monk robe bottom prayer bonus 5.                                                | Direct object evidence resolves all 86 runtime equipment identities and matches 81 rows exactly. Focused equipment impact shows the explicit Unholy symbol bonuses change representative DPS by about +0.15% to +0.55%, while the Monk robe bottom prayer correction changes the prayer-supply trip economy by about 29%; source-derived equipment alch differences do not affect current calculations. D-018/D-028 prefer documented source values over legacy omissions or manual approximations. This accepts the in-memory candidate deltas, not committed snapshot wiring or the root runtime switch.                                            |
| D-057 | 2026-07-09 | Accept Revision 274 weapon/ammo fields and source-cost fallbacks as generated candidate truth, including explicit thrown-weapon ranged attack bonuses omitted by legacy index-sim.                                                    | All 35 weapon, 18 ammo and 19 spell runtime identities resolve to pinned source rows; spell calculation fields match exactly. Focused impact confirms that Bronze dart `rangeattack=3` changes representative DPS by about +1.25% and Rune knife `rangeattack=25` changes the high-defence representative by about +28.41%. The existing combat model keeps source `rangeattack` as weapon accuracy and source `rangebonus` as the ammo/ranged-strength component, so the delta is not duplicate counting. This accepts the candidate data interpretation and source-derived fallbacks, not a combat-formula change or browser runtime switch.        |
| D-058 | 2026-07-09 | Keep Revision 274 cut and uncut gem object ids as distinct item identities; use legacy gem aliases only as exact-key-missing price fallbacks.                                                                                         | The pinned source contains separate `sapphire` and `uncut_sapphire` objects (and equivalent emerald/ruby/diamond pairs), so collapsing them as one canonical item would discard real source identities and can value cut-gem drops with uncut prices. Exact price keys now win; the legacy `market.js` aliases remain available only when the requested exact key is missing. Alias-map self-collision gates remain, but source item catalogs may contain both legitimate ids. This narrows D-052 now that authoritative source evidence exists without changing scheduled market provider policy.                                                    |
| D-059 | 2026-07-09 | Switch the root Vite runtime from the legacy-derived static bridge to the committed Revision 274 source-backed generated snapshot.                                                                                                    | The raw generator now resolves every expected monster/item/weapon/ammo/spell/equipment identity, extracts all 63 core-loot tables, passes schema/hygiene gates, reports runtime readiness with zero blockers and passes all 10 representative impact cases with D-055/D-057 accepted deltas. This satisfies D-054's deferred switch criteria and advances the project's primary rewrite goal. `src/adapters/static-runtime` and legacy source execution remain only for regression, reference, bridge freshness and rollback evidence; this decision does not approve legacy deletion, quest/clue expansion or generated requirement skill inference. |
| D-060 | 2026-07-10 | Retain the archived index-sim implementation and legacy-derived artifacts as regression, reference and historical evidence; do not start legacy deletion in the current rewrite scope.                                                | The root app and its user-facing workflows now run on the TypeScript/Vite rewrite and the source-backed generated snapshot, so deleting the archive adds no current user value. The archived engine still supports golden comparison, bridge freshness, migration evidence and diagnosis of intentional rewrite deltas. It is not a supported runtime, product truth source or reason to add compatibility shims. Reconsider removal only through a later explicit decision after any still-needed legacy-executing checks have app-owned fixture replacements.                                                                                       |

| D-061 | 2026-07-10 | Use the first-party 2004Scape `GET /api/hiscores/player/:username` JSON endpoint as the V1 authoritative Hiscores source behind the repo-owned server-side provider contract. | The 2004Scape Hiscores API announcement from 16 March 2025 explicitly replaces HTML scraping, documents player rows, skill type ids 1-7, stored XP units and upstream rate limits. The provider keeps the browser same-origin, fixes the upstream HTTPS origin, rejects redirects, bounds response size, maps only the seven accepted combat skills and retains the existing manual fallback. This resolves the source portion of D-022 without choosing a production host or access-log retention policy. |
| D-062 | 2026-07-10 | Derive scheduled market prices from the first `soldListings` page of each approved `markets.lostcity.rs/items/{slug}` mapping, using completed coin-denominated buy and sell rows as unweighted per-listing observations. | The global sales feed is too sparse across supported items. One bounded ten-row item page per allowlisted mapping gives item-specific evidence without following pagination indefinitely. Current live rows use `price: null` and encode the per-item GP amount as one `coins` item in one offer; the adapter also accepts an explicit positive `price` when present. Item swaps, mixed offers and ambiguous multiple offers are skipped instead of assigned a guessed GP value. The writer uses MAD filtering for at least five observations, a median-ratio guard for three or four, averages at most the newest five accepted rows, retains the prior price below three accepted rows or when the latest accepted row is older than 30 days, and excludes rows older than 90 days. Quantity remains diagnostic and does not multiply an observation. |
| D-063 | 2026-07-10 | Generated Revision 274 item data is authoritative for high-alch values; market snapshots and imported or legacy PriceSets may change market prices but cannot override generated alch values. | High alch is game data derived from source object cost, not a market observation. Keeping `alch.json` authoritative would preserve the ambiguous legacy ownership the rewrite is replacing. `alch.json` remains compatibility/regression evidence for now, while runtime composition, selected imports, legacy migration, exports and calculation-impact work use generated alch values. |
| D-064 | 2026-07-10 | Expose committed `price-history.json` in Economy as read-only shared history beside separate browser-local comparison snapshots. Keep 12-hour points for 90 days and one latest point per older UTC day. | Users need long-term in-app price tracking without a database or user-triggered upstream fetch. Shared history is loaded from the static artifact and never written to localStorage; Save local comparison and Clear local history affect only `index-sim:price-history`. Daily compaction preserves the long trend while bounding repository growth better than retaining every twice-daily point forever. |
| D-065 | 2026-07-10 | Do not persist Hiscores player names, query strings, raw URLs, request bodies, headers, IP addresses or upstream payloads in application/observability logs. Production access logs must strip the query string or redact `player`; unavoidable provider metadata uses the shortest configurable retention, no longer than seven days, with operator-only access. | Player names are untrusted request data rather than account data, and the current same-origin API uses a GET query parameter that common hosts may otherwise record. Provider-neutral telemetry may retain only the route template, response status/error category, latency class and generated request id. This policy avoids creating a player-search history, requires no database and lets host selection test a concrete privacy constraint instead of reopening the policy decision. |
| D-066 | 2026-07-11 | Deploy the root-path Vite app and same-origin Hiscores API as one Cloudflare Worker with Static Assets, using Workers Builds to deploy every validated `master` commit and provider preview URLs before any custom domain. | One Worker keeps UI/API routing, security headers, rollback and market-commit propagation in one release unit. `/api/*` runs Worker-first while static misses use SPA fallback, and unknown API routes return JSON 404 instead of `index.html`. Workers Logs observability and Logpush are disabled under D-065, no runtime secret/database/auth/tenant state is added, and exact Wrangler 4.109.0 commands keep provider tooling reproducible. Cloudflare account/Git connection, preview/production smoke and any later custom domain remain external deployment evidence rather than repository implementation. |
| D-067 | 2026-07-11 | Define completion at repository handoff readiness: no current maintainer Cloudflare account, public deployment or observed production cron is required. A future adopter owns service activation and operational evidence by following the committed runbooks. | The project goal is a complete, portable rewrite rather than operation of a specific public instance. The repository includes the provider adapters, Cloudflare runtime/configuration, scheduled writer, validation commands, security boundaries and rollback/evidence checklists needed by an adopter. This closes implementation backlog without weakening claim gates: live Hiscores may be claimed only after deployed routing/privacy/live smoke, and scheduled-current prices only after a successful configured cron. |
| D-068 | 2026-07-11 | Duel setup review compares normalized active setup fields and snapshot-minus-live calculated results under one shared current target, cannon, loot policy and PriceSet; it does not attribute an output delta to an individual changed field. | The simulator can reliably recompute complete setups but cannot isolate interacting field effects without a separate controlled-attribution model. Comparative wording gives users auditable setup and impact evidence without inventing causality. Snapshot schema version 1 remains unchanged, calculated output remains transient and Planner targets plus inactive per-style caches stay outside the diff. |
| D-069 | 2026-07-11 | Stats source distributions are event-scoped: one hit within a supported special attack and one fired cannonball. Do not infer aggregate multi-hit special, kill, trip or hourly distributions in this slice. | Existing results provide exact inputs to the accepted single-event bucket model: special hit chance/max hit/expected damage per hit and cannon hit chance/max ball/damage per fired ball. Aggregate distributions would require new independence, timing and encounter assumptions. Explicit scope labels add truthful analysis without creating a second formula path. |
| D-070 | 2026-07-11 | Use a roving ARIA tablist for the Workbench, one dynamic labelled tabpanel, a first-focus skip link and roving focus for visible Dense monster rows. Keep this as a bounded keyboard interaction contract rather than a WCAG conformance claim. | The previous shell required tabbing through every workbench button and every monster row, while Dense row focus explicitly removed its outline. Standard arrow/Home/End patterns, one sequential Tab stop per composite widget and visible focus remove those concrete barriers without changing domain behavior, persistence or the visual layout. Certification still requires a separate human/assistive-technology audit. |
| D-071 | 2026-07-11 | Accept pinned Revision 274 `levelrequire` RuneScripts as the numeric item-skill requirement source and NPC config `size` with default 1 as the selected-target dragon halberd size source. | The parser maps reviewed Attack, Strength, Defence, Ranged and Magic label families, fails unknown/conflicting trigger shapes and excludes quest completion state while retaining their explicit numeric gates. The generated runtime contains 94 requirement rows and size for 63/63 monsters. Dragon halberd uses one selected-target hit at size 1 and two above size 1; the source script's possible adjacent size-1 target is outside this single-target simulator. Missing-size legacy contexts retain D-050 fallback/warning, and D-051 remains for legacy or missing requirement rows. The representative impact suite accepts the Rock Crab delta without changing archived legacy golden fixtures. |

## Recommended, not final implementation decisions

| ID    | Recommendation                                                                                                                                                                 | Status                                                          |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| R-001 | Use TypeScript strict mode, React, Vite, Vitest, Playwright and Zod for the rewrite.                                                                                           | Recommended until a human confirms or changes the target stack. |
| R-002 | Keep simulation/domain work static-first and avoid choosing Next.js/FastAPI. D-066 later selected a narrow Cloudflare Worker only for same-origin Hiscores and static hosting. | Superseded in part by D-066; domain remains static-first.       |
| R-003 | Make domain modules free of React, DOM, `window`, `fetch` and `localStorage`.                                                                                                  | Recommended architectural boundary.                             |

## Open decision boundaries

- Package manager: npm is used for the initial scaffold; changing package manager remains a future explicit decision.
- Backend/runtime: D-066 accepts the Cloudflare Worker production runtime and D-067 treats repository handoff readiness as complete. A future adopter may connect an account and collect privacy/routing/live evidence before claiming public availability. Manual levels remain the fallback. Market refresh stays scheduled repo automation; D-044 keeps archived legacy shims out of the rewrite.
- Database: no decision to add one; market price refresh is explicitly file-backed JSON.
- Deployment: D-066 fixes the optional Cloudflare production path, while D-067 leaves account ownership, public activation and any custom domain to a future adopter. They are runbook steps, not unfinished repository implementation.
- D-066 implements Cloudflare static/API security headers; effective preview and production responses still require deployed smoke evidence.
- Source data workflow: LostCityRS/Content Revision 274 is the accepted root runtime source. `npm run data:generate` reads the pinned repository-local raw checkout, writes `source-pin.json`, `game-data.json` and the current revision-impact report, and the root app consumes that committed generated snapshot under D-059. The raw parser resolves all expected runtime identities and 63/63 core-loot tables; normalized source-slice fixtures remain contract-test inputs only. Historical source-backed snapshot files are not part of the accepted model.
- Legacy retention: D-060 keeps the archived implementation and legacy-derived artifacts as regression, reference and historical evidence outside the supported root runtime. Legacy deletion is not active work; reopening it requires an explicit decision and app-owned replacements for any legacy-executing checks that are still needed.
- Archived data duplicate policy: D-045 keeps duplicate object keys in archived `gamedata.js` out of standalone merge-blocking gates. Resolve canonical duplicate values through generated/source-backed data, not by hand-selecting values inside legacy JS.
- Canonical item/price alias normalization: D-058 applies authoritative Revision 274 item identities. Cut and uncut gems remain separate items; exact price keys win and D-052 legacy aliases are fallback-only when the exact key is missing. Alias-map self-collisions remain invalid.
- Safespot default UX: D-046 accepts current ranged, magic and halberd auto-safespot behavior as the V1 default with user override. Source-backed encounter/NPC data may reopen this later, but it is not a current release blocker.
- Dragon halberd NPC-size fallback: D-071 accepts source-backed NPC size and selected-target hit behavior for the generated runtime. D-050 now remains only for missing-size legacy contexts, which preserve the visible double-hit fallback warning.
- Game data refresh workflow: revision bumps are accepted only through reviewed development PRs with generated snapshot diffs, validation, test/golden impact and explicit accepted calculation deltas. Automatic main-branch game-data updates are not accepted.
- Generated snapshot scope: v1 includes only current simulator-consumed domain data: monsters, items, equipment/weapons/ammo/spells, drop rows, planner/loadout requirements and provenance/warnings. It excludes raw upstream content, unused NPC/dialog/quest/world/map data, historical revisions and speculative future fields.
- Revision impact gate: `docs/project/revision-impact/current.md` must be committed in any revision bump PR and must summarize source refs, generator metadata, generated data diffs, representative calculation diffs, intentional deltas and validation results. Changed DPS, kills/hr, XP/hr, GP/hr or GP/XP outputs must be accepted or fixed before merge.
- Calculation-impact suite: the merge-blocking report section uses a curated representative suite seeded from existing golden/parity fixtures plus focused generated-data cases. A broad all-monster scan is included as an informational section and does not automatically block merge. The scan uses one fixed simple melee, ranged and magic baseline, with accepted notable-outlier thresholds for DPS/kills/hr/XP/hr over 10%, GP/hr or GP/XP over 25%, missing required data, warning-count increases and monsters entering/leaving the scan. The active raw Revision 274 generator and `data:source-impact` share the source parser and impact core; the committed report passes 11/11 representative cases and records 22 informational outliers under D-055/D-057/D-071.
- Price update workflow: `markets.lostcity.rs`, scheduled-only refresh, latest market-price JSON and retained shared history are accepted. D-062 fixes the source contract to one first-page item response per approved mapping with robust completed-trade estimation; D-063 keeps high alch in generated game data; D-064 exposes shared history read-only. The scheduler uses GitHub Actions cron, `GITHUB_TOKEN` with `contents: write`, no manual dispatch and commit-if-diff wiring for only `prices.json` and `price-history.json`. Public crawler policy, the current item catalog/offer shape and a full 80-mapping live dry-run are evidenced. D-053 keeps the remaining root `MARKET_PRICES_UPSTREAM_URL` configuration and first successful scheduled-run evidence out of the V1/trusted-tester gate, but they are still required before claiming scheduled-current market prices.
- Live integrations: [../technical/live-integrations-spec.md](../technical/live-integrations-spec.md) owns the target contract for hiscores and market prices. D-061 accepts the first-party 2004Scape Hiscores JSON API and D-062 fixes the parsed market page shape. D-067 classifies deployed Hiscores and first configured scheduled-run evidence as adopter operations required only for the corresponding public freshness/availability claims.
- Hiscores upstream: D-061 accepts `https://2004.lostcity.rs/api/hiscores/player/:username` behind the repo-owned server provider. D-065 fixes the no-player-name/query logging and maximum seven-day unavoidable metadata policy. D-066 selects the Cloudflare Worker same-origin runtime; D-067 leaves account connection and deployed verification to the adopting operator.
- UI language policy: existing UI strings are English; documentation is now mostly Finnish.
- Rewrite setup persistence is versioned; a focused `sim_input_v3` setup-migration foundation is captured, while unsupported broader legacy `localStorage` migration fixtures remain open only if a later explicit decision raises them.
- Legacy Migration V1 implementation: D-042 accepts compatible import for the audited nested `sim_input_v3.monsterSetups` and `sim_input_v3.cannonByMonster` fields. Exact compatible field mapping, unknown-id handling and validation details belong to the implementation/test pass. D-048 keeps `sim_planner_v1` review-only/not migrated for V1, and D-049 keeps full legacy price-history payloads review-only/not migrated for V1.
- Cannon occupancy/overlay, cannonball cost and cannon ranged XP are ported into pure `src/domain/trip`; the dense rewrite dashboard exposes per-monster cannon controls, while final full-workbench placement remains open.
- Legacy source files are retained for fixtures, reference comparison and legacy-derived static snapshot regeneration. D-059 completes the authoritative source-backed generated-runtime switch; deleting the legacy/reference assets still requires separate full-replacement evidence and an explicit deletion decision.
- The current composed UI-facing result contract is `FullSimulationResult`; whether a later public contract should be renamed back to `SimulationResult` remains open.
- Dense spreadsheet can become the default root view during parity work; its final coexistence with the full tabbed workbench remains open.
- Planner item requirements are consumed from the D-071 generated snapshot requirements when present, including Strength; D-051 keeps the manual fallback table for legacy snapshots and current items without a generated row. Removing it later requires an explicit coverage/quest-state decision.
- D-047 keeps future/hypothetical planner gear out of V1. Canonical Planner pools stay current-revision-only, source-backed or snapshot-backed, and non-hypothetical until a future explicit product/data provenance decision reopens that scope.
- Historical multi-revision simulation is not accepted yet; the app targets one current accepted revision at a time.
