# Rewrite vs legacy parity report

- Status: current audit snapshot
- Date: 2026-07-05
- Owner: technical docs
- Scope: calculation reliability of the Vite/React rewrite against the archived legacy `SimEngine.simulate()` baseline.

## Executive summary

The rewrite has strong parity evidence for the combat/equipment core numbers that users usually notice first: max hit, hit chance, average hit, DPS, effective DPS and special-attack fields. These are compared against all 18 legacy golden fixtures with `0.000001` numeric tolerance after 6-decimal rounding.

Trip, loot and supply parity is also good for every fixture currently wired into `src/tests/trip-loot-supply.test.ts`. Those tests cover GP/hr, net GP/hr, supply costs, prayer XP, trip bound, incoming damage, banking efficiency, top loot rows, cannon occupancy and cannonball supply for melee, ranged and magic cases.

XP/hr now has direct fixture evidence for 17 current-behavior cases through `src/tests/xp-parity.test.ts`, including cannon ranged XP as a separate effective XP row, and UI view-model summary numbers are snapshot-compared against default melee, ranged safespot and cannon-enabled ranged paths. The ring-of-recoil XP/hr exclusion has been localized to XP attribution: legacy includes recoil DPS in the non-XP damage share before computing combat XP, while the rewrite combat result does not know about recoil because recoil is added later by the trip module. The main remaining reliability caveats are the recoil XP attribution decision, final cannon tab/workbench placement, broader browser-displayed numeric snapshots, planner legacy parity, live upstream parity for market/hiscores and legacy `localStorage` migration.

## Evidence used

| Evidence                                | What it proves                                                                                                                                                                                                                                                          | Limit                                                                                                                                  |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `src/tests/fixtures/legacy-golden.json` | 18 frozen current-behavior cases from `SimEngine.simulate()` using `gamedata.js`, `engine.js`, `trip.js` and `equipment.js`.                                                                                                                                            | Excludes `market.js`, `planner-core.js`, `views.jsx`, `planner.jsx`, live network and browser `localStorage`.                          |
| `src/tests/legacy-golden.test.ts`       | Legacy fixture baseline is still reproducible.                                                                                                                                                                                                                          | Does not compare rewrite to legacy.                                                                                                    |
| `src/tests/domain-core.test.ts`         | Rewrite combat/equipment matches legacy fixture fields for all 18 cases.                                                                                                                                                                                                | Does not claim parity for trip/economy/UI persistence; cannon overlay is covered by the trip parity slice.                             |
| `src/tests/trip-loot-supply.test.ts`    | Rewrite trip/loot/supply matches legacy fixture fields for all 18 cases, including cannon occupancy/overlay and cannonball supply.                                                                                                                                      | Does not prove final legacy Cannon-tab placement or browser-rendered numeric display parity.                                           |
| `src/tests/xp-parity.test.ts`           | Rewrite combat XP/hr fields match legacy fixture fields for 17 cases, cannon ranged XP is modeled as a separate effective XP row, and a diagnostic test localizes the recoil exclusion to direct-damage attribution.                                                    | Excludes only the ring-of-recoil XP/hr fixture from pass parity; `totalXpPerHour` is only compared when all XP rows are rewrite-owned. |
| `src/tests/planner-domain.test.ts`      | Rewrite planner is deterministic, uses domain APIs and covers known stance/`accByType` regressions.                                                                                                                                                                     | Planner golden cases are rewrite-domain golden summaries, not legacy planner parity.                                                   |
| `src/tests/ui-view-model.test.ts`       | UI view models compose domain results, keep form state separate from `SimulationRequest`, map valid melee/ranged special attack selections into requests, compare summary numbers for default melee and ranged safespot fixtures, and verify cannon changes visible rates. | Does not snapshot every displayed browser value or every user workflow.                                                                |
| `src/tests/e2e/scaffold.spec.ts`        | Browser app renders the dense spreadsheet root, responds to combat-style switching, checks full monster table row count, sorting and target selection, covers special attack controls/metrics, per-monster cannon controls, mocked hiscores and market sync flows, and checks the disabled service copy. | Smoke test only; it does not compare displayed numbers to legacy or call live upstream services.                                       |

## Status meanings

- `pass`: Direct rewrite-vs-legacy parity exists for the relevant numeric fields in the current fixture scope.
- `partial`: Some evidence exists, but coverage is incomplete, indirect or not UI-level.
- `fail`: A known mismatch is currently proven by tests or audit evidence.
- `not ported`: No maintained rewrite path exists yet for the behavior.

No committed failing parity tests are left in this audit. The main gaps are `partial` or `not ported`; the ring-of-recoil XP/hr fixture is an explicit XP parity exclusion with status `needs decision`.

## Parity matrix

| Area                          | Status       | Current evidence                                                                                                                                                                                                                                                                      | What the user can trust now                                                                                                                                                                                                      |
| ----------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Melee                         | `partial`    | Combat/equipment parity across all melee golden cases; trip parity includes every non-cannon melee case; XP/hr parity covers melee cases except the ring-of-recoil fixture.                                                                                                           | Melee max hit, hit chance, DPS and non-recoil tested trip/economy/XP fields should match legacy in covered setups.                                                                                                               |
| Ranged                        | `partial`    | Combat/equipment parity covers ranged bow/thrown/special/cannon fixture inputs; trip parity covers all ranged fixtures including cannon occupancy and cannonball supply; XP/hr parity covers ranged fixtures including the cannon XP row.                                             | Ranged combat, XP/hr and tested ammo/supply paths are reliable in fixture-covered setups. Cannon settings controls and displayed overlay remain a UI parity blocker.                                                             |
| Magic                         | `partial`    | Combat/equipment parity covers bolt/wave/god spell/poison fixtures; trip and XP/hr parity cover all magic fixtures.                                                                                                                                                                   | Magic combat, rune-cost, alch-related trip values and tested XP/hr fields are reliable. Full total-XP rows with non-combat XP remain partial.                                                                                    |
| Equipment                     | `pass`       | `domain-core.test.ts` checks bonus summing, two-handed shield exclusion, thrown ammo handling and loadout-to-combat bonus mapping.                                                                                                                                                    | Equipment bonuses used by combat are covered for the tested item paths.                                                                                                                                                          |
| Hit chance                    | `pass`       | Direct formula unit tests plus all 18 fixture comparisons.                                                                                                                                                                                                                            | Hit chance should match legacy for covered combat styles and stance paths.                                                                                                                                                       |
| Max hit                       | `pass`       | Direct formula unit tests plus all 18 fixture comparisons for `maxHit` and `peakMaxHit`.                                                                                                                                                                                              | Max hit and boosted peak max hit are high-confidence for covered cases.                                                                                                                                                          |
| Attack speed                  | `partial`    | DPS parity indirectly exercises attack speed; loadout mapping tests check `attackSpeed` for some equipment.                                                                                                                                                                           | Attack speed-dependent DPS is covered, but attack speed itself is not directly fixture-compared as a standalone UI number.                                                                                                       |
| DPS                           | `pass`       | All 18 fixture cases compare `dps` and `effDps`.                                                                                                                                                                                                                                      | DPS/effective DPS are the strongest parity area.                                                                                                                                                                                 |
| XP/hr                         | `partial`    | `src/tests/xp-parity.test.ts` compares `xpPerHour`, `effectiveXpPerHour` and rewrite-owned `skillXpBreakdown` rows for 17 fixtures, models cannon ranged XP as a separate effective row, and diagnoses the recoil exclusion.                                                          | XP/hr is reliable for covered non-recoil cases. Recoil XP/hr needs a decision on whether XP attribution should include trip-layer recoil damage like legacy. Full `totalXpPerHour` rows with prayer/alch XP remain open.         |
| GP/hr                         | `partial`    | Trip parity compares `gpPerHour`, `netGpPerHour` and `effectiveNetGpPerHour` for all 18 cases.                                                                                                                                                                                        | GP/hr is reliable for fixture cases, including cannonball costs, but not live market paths.                                                                                                                                      |
| Loot value                    | `pass`       | Trip parity compares `gpPerKill` and top loot rows for all 18 cases, including high-value, low-value and herb/inventory pressure.                                                                                                                                                     | Tested loot EV and top loot ordering should match legacy for fixture-covered setups.                                                                                                                                             |
| Alch                          | `pass`       | Trip parity includes alch fixture paths and compares top loot row `pref`, `evGp`, `prayerXp`, `slotFrac` and `alchValue`.                                                                                                                                                             | Tested alch decisions and values are reliable.                                                                                                                                                                                   |
| Supply cost                   | `partial`    | Trip parity compares food, potion, ammo, rune, cannonball and total supply cost per kill for all 18 cases.                                                                                                                                                                            | Regular supply costs and cannonball supply cost are reliable in fixture-covered cases.                                                                                                                                           |
| Trip duration                 | `partial`    | Trip parity compares `killsPerTrip`, `bound`, `efficiency`, `effectiveKph`, `bankSeconds`, incoming damage and slots for all 18 cases.                                                                                                                                                | Tested food-, loot-, inventory-, cannon- and prayer-related trip behavior is reliable for fixture-covered setups.                                                                                                                |
| Trip-limited                  | `pass`       | Fixture coverage includes food-limited and recoil/dragonfire/incoming-damage trip cases.                                                                                                                                                                                              | Tested trip-limited outcomes are reliable.                                                                                                                                                                                       |
| Inventory-limited             | `pass`       | Fixture coverage includes non-stackable loot pressure and loot-capacity comparisons.                                                                                                                                                                                                  | Tested inventory-limited outcomes are reliable.                                                                                                                                                                                  |
| Planner                       | `partial`    | Rewrite planner has domain golden tests and fixes known candidate-weapon stance/`accByType` risks.                                                                                                                                                                                    | Rewrite planner is internally deterministic and better bounded, but not proven numerically equivalent to legacy `planner-core.js`.                                                                                               |
| UI summary                    | `partial`    | UI view-model tests compare summary values for default melee and ranged safespot fixtures, verify special attack request/metric mapping and verify cannon-adjusted visible rates; Playwright smoke checks the dense spreadsheet shell, combat-style switching, full-table row count, sorting, target selection, special attack controls and cannon controls. | The selected UI view-model summaries match legacy fixtures, special attack and cannon controls are smoke-covered, and the root dense table workflow is smoke-covered. Browser-rendered numeric snapshots and broader workflows still need coverage. |
| Browser price import          | `partial`    | `ui-adapters.test.ts` validates imported `PriceSet` JSON and malformed import rejection.                                                                                                                                                                                              | Static/imported price validation is safer than legacy. Live market parity still depends on approved upstream/runtime evidence.                                                                                                   |
| Live market sync              | `partial`    | Same-origin API, browser adapter, UI state tests and Playwright mocked sync cover validation, disabled default provider, current-monster/all-supported sync and partial reports.                                                                                                      | The rewrite owns a safer validated service boundary and UI state. It does not prove equivalence to a live upstream or legacy `market.js` scrape behavior.                                                                        |
| Hiscores/backend              | `partial`    | Same-origin API, browser adapter, UI state tests and Playwright mocked lookup cover validation, disabled default provider, preview and apply behavior.                                                                                                                                | The rewrite owns a safer validated service boundary and UI state. It does not prove equivalence to a canonical live hiscores upstream.                                                                                           |
| Legacy localStorage migration | `partial`    | Rewrite detects known old keys, shows a user-facing import/keep/clear flow, imports compatible `sim_input_v3` setup fields into `index-sim:rewrite-setup`, compatible `sim_hiscore_player` into `index-sim:hiscores:last-player`, and compatible legacy price/alch maps as an explicit `PriceSet`. Unit tests cover invalid legacy state, invalid rewrite envelopes/data and rewrite version mismatch. Playwright smoke covers Import preserving legacy setup, price, history and hiscores keys; Keep preserving legacy data; and Clear removing only known keys after confirmation. | Full planner, custom setup, loot prefs, compare state, cannon map, hidden tiers and legacy price-history migration are not ported. Rewrite setup version 1 envelopes are still not implicitly migrated.                           |

## Fixture coverage

The legacy baseline currently has 18 cases:

- 8 melee cases covering rune scimitar, dragon weapons, poison, special attacks, recoil, alch, food-limited trips, low-value loot and dragons.
- 5 ranged cases covering bow, thrown weapon, safespot, ammo recovery/no recovery, special attack and a cannon fixture input.
- 5 magic cases covering bolt/wave/god spells, charge, alch, rune cost, poison and safespot behavior.

Trip/loot/supply parity currently covers all 18 cases, including the cannon fixture's occupancy/overlay behavior and cannonball supply accounting.

XP parity currently covers 17 of these 18 cases. The excluded case is:

- `melee_ring_recoil_fire_giant_food_trip`: diagnostic coverage shows the trip/KPH/recoil damage fields match legacy, but XP/hr does not. Legacy scales combat XP by an XP direct fraction of about `0.971821` because recoil contributes to kill DPS but grants no XP. The rewrite combat result reports `directDamageFraction = 1` for this case because recoil is calculated later by the trip module. This is an `intentional delta candidate` / `needs decision` before the fixture can be returned to pass parity.

## Known intentional baseline behavior

Current golden fixtures preserve legacy behavior before bug fixes. These are not accepted permanent product decisions:

- Dragon halberd special still follows the legacy small-target behavior because NPC size data is missing.
- Ranged, magic and halberd auto-safespot behavior is preserved in fixtures.
- Ring of Wealth scope follows current `GameData.adjustForRoW` behavior.
- Known market and legacy planner bugs are outside the `SimEngine.simulate()` golden fixture set.

See [../project/bug-triage.md](../project/bug-triage.md) for the current list.

## Release blockers before full replacement

- Decide whether ring-of-recoil XP/hr should preserve legacy XP attribution by sharing trip-layer recoil damage with XP calculation, or remain an intentional rewrite delta.
- Decide final Cannon-tab/workbench placement and add browser-rendered numeric snapshots if full displayed cannon parity is required.
- Extend UI-level numeric regression checks beyond the default melee and ranged safespot view-model summaries if full browser-display parity is required.
- Decide whether planner needs legacy parity fixtures or whether the rewrite planner is an accepted intentional replacement.
- Decide remaining legacy `localStorage` migration/reset policy beyond the current setup, hiscores last-player and compatible price/alch import flow.
- Replace the raw legacy data bootstrap with an authoritative generated `GameDataSnapshot` before deleting legacy data sources.
- Choose the authoritative market and hiscores upstreams, production runtime/hosting and provider wiring before claiming live upstream parity for market or hiscores.

## Security and privacy notes

This report uses committed fixtures and source-controlled tests only. It does not use player names, browser `localStorage`, live hiscores, live market requests, secrets or machine-local data.

The legacy archive may still contain references to `/api/*`, `run_sim.py`, CDN scripts and browser globals. Those are not part of the root Vite production path, and they are not parity evidence for the rewrite UI. The production rewrite service copy is driven by same-origin status endpoints and the disabled default provider rather than local `run_sim.py` instructions.

## Recommended next parity goals

1. Decide the ring-of-recoil XP attribution policy, then either implement legacy parity or record an accepted intentional delta.
2. Decide final cannon workbench placement and add browser-rendered cannon numeric snapshots if required.
3. Add browser-rendered numeric snapshots if UI formatting/display parity is required beyond the view-model layer.
4. Decide whether planner parity means matching legacy output or accepting the rewrite planner as a corrected replacement.
5. Decide whether legacy planner, custom setup, loot prefs, compare state, cannon map, hidden tiers and full price-history keys should be migrated or intentionally left behind.
